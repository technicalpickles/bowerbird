{
  "schema_version": 1,
  "solo_p99_nanos": 645682,
  "fanout3_p99_nanos": 742273,
  "burst_p99_nanos": 0,
  "steady_p99_nanos": 830509,
  "samples": 200,
  "absolute_budget_nanos": 100000000,
  "regression_max_ratio": 2.0,
  "_seeding_note": "Seeded 2026-07-31/2026-08-01 (Story 5.5, maintainer decision): solo/fanout3/steady ARMED at per-shape worst across EIGHT green n=200 ubuntu-latest runs spanning three windows (five Story 5.18 no-change calibration runs 30599853185/30600064861/30600097130/30600129141/30600163266 plus organic runs 30637058391, 30680431096, 30680620908; observed solo 0.394-0.646ms, fanout3 0.416-0.742ms, steady 0.659-0.831ms), ratio 2.0 per the 5.18 macOS methodology. burst is DELIBERATELY 0 (regression gate auto-skips for burst only; the 100ms NFR2 absolute gate still enforces it): ubuntu-latest has an intermittent per-event ~40ms stall whose RATE varies by run -- zero stalls in all eight seeding-pool runs (burst 4.092-12.595ms), then ~0.5-1% of events on run 30681640759 the same night (solo p99 0.426ms but max 41.1ms; burst worst-of-8 amplifies any stall rate, p99 41.1/92.7ms across two attempts, failing a 25.2ms threshold on a daemon-neutral tree twice). No seed+ratio both covers the stall state (up to 92.7ms observed) and still detects a real regression under the 100ms ceiling, so burst stays unarmed on Linux until the stall is understood (deferred-work 'Deferred from: Story 5.5' 2026-08-01 entry has the characterization; the old all-shapes ~40ms punt entry is superseded by this per-shape one). solo/fanout3/steady held comfortably in BOTH runner states (worst observed in the stall state: 0.455/0.546/0.742ms vs thresholds 1.29/1.48/1.66ms). Do NOT chase a future red by bumping the armed seeds upward; recalibrate the ratio from fresh multi-window spread instead."
}
