// Lint self-test fixture: fully-qualified path violation.
// A `rusqlite::Connection::open` call OUTSIDE `crates/daemon/src/db/pool.rs`.

fn _violation_fully_qualified() {
    let _ = rusqlite::Connection::open("/tmp/foo");
}
