// Lint self-test fixture. Deliberate violation: a `rusqlite::Connection::open`
// call that lives OUTSIDE `crates/daemon/src/db/pool.rs`. The CI lint script
// must reject this file when included in its scan.

fn _violation() {
    let _ = rusqlite::Connection::open("/tmp/foo");
}
