# Story 5.8: Server-side session filter

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a presenter author,
I want `GET /sessions` to accept `?state=`/`?since=`/`?limit=` query filters AND the WS snapshot-on-subscribe burst to stop blasting me with the full `Ended` graveyard,
so that a new presenter can fetch (and a reconnecting presenter can be handed) just the sessions it cares about — active ones — instead of re-implementing hide-ended client-side and paying the cost of every dead session the daemon has ever seen.

**Closes dogfooding Finding 5 (filter half)** from `docs/dogfooding-feedback.md` (2026-06-01, "presenters can only triage on what the wire carries"). Building the pickletown web `/sessions` triage radar against `bowerbird-deck` surfaced that `Ended` is non-terminal by design (ADR 0004: `claude --resume` revives it), so the daemon cannot delete-on-death, and `SELECT_NON_SENTINEL_SESSIONS` returns the **entire** session history — dumped on every presenter both via the REST `GET /sessions` response AND via the snapshot-on-subscribe burst (Story 2.3). Concrete signal: a "134 ended hidden" footer in the deck, and two independent presenters (deck + web) each re-implementing the same hide-ended filter client-side. This story moves that filter server-side.

**Operationalizes ADR 0008** (`docs/decisions/0008-server-side-session-filter.md` — authored 2026-06-02, Status Accepted). Read it first. The decision: filter on **both** surfaces by a **presenter-supplied predicate** (the read-time `current_state`), defaulting to unfiltered so v1.0 behavior is preserved everywhere. REST gets `?state=`/`?since=`/`?limit=` query params; the WS snapshot-on-subscribe burst is scoped by an optional `states` field on `ClientMessage::Subscribe`. The daemon never decides on its own what is "worth" sending — the presenter expresses intent and the daemon filters by a mechanical fact, the same way `topic` already scopes by session id. This keeps the substrate a pure fact-filter (Axiom 1).

Source proposal: `docs/bmad/planning-artifacts/sprint-change-proposal-2026-06-01-dogfood-triage.md` §4.2 (the change list — note: §4.2 said "no ADR" assuming REST-only query parsing; the maintainer's choice to scope the WS snapshot by a presenter-supplied predicate modifies the wire protocol, which ADR 0008 records) and §3 (rationale: why filter-only, not a retention sweep). **Second of four dogfood-triage stories (5.7–5.10)**; all four gate the v0.1.0 tag (now Story 5.14). Story 5.7 (session cwd + started_at on the wire) is **done** and is the immediate predecessor — it touched almost every file this story touches.

**This is filter-only, NOT a retention sweep.** The `Ended` graveyard never *evicts* (managed truncation = the no-list "no `bowerbird gc`" cut). This story makes the graveyard *filterable* and lets presenters opt the snapshot burst out of it; it does NOT delete projection rows. Tracked as bean `gt-3cnt` — this story **partially resolves** it (filter half); the retention sweep stays open on the bean as a post-V1 deferral (`deferred-work.md`).

**This story modifies the wire protocol** (`ClientMessage::Subscribe` gains an optional `states` field). Consequences the dev must handle: the protocol-changelog gate fires (a `type: schema` entry satisfies it), ADR 0008's `Affects context.md sections` requires a same-PR `project-context.md` touch, and the additive-compat policy applies (the field is `#[serde(default)]` so old presenters omitting it are unaffected). The REST `?state=`/`?since=`/`?limit=` filters are query params parsed by an axum `Query` extractor (NOT new `protocol` types) — they ride the same changelog entry as additive behavior. **No schema migration** — `events`/`session_projections` are unchanged; this is a query/transport-shape story.

## Acceptance Criteria

1. **Given** `GET /sessions` with no query params **When** the daemon serializes the response **Then** the behavior is byte-for-byte identical to pre-5.8 (the full non-sentinel session list, `ORDER BY updated_at DESC, source ASC, session_id ASC`, each row carrying `cwd`/`started_at`/`last_pid` from Story 5.7) — the default is unfiltered and preserves current behavior exactly.

2. **Given** `GET /sessions?state=<csv>` where `<csv>` is a comma-separated list of one or more session-state tokens (case-insensitive; accepted tokens are the `SessionCurrentState` wire variants `idle`, `working`, `waitinginput`, `ended`, `unknown`) **When** the daemon builds the response **Then** only sessions whose **read-time-derived** `current_state` (the value the response actually carries, after `current_state_for_read`'s stale-`Working`→`Idle` fallback — Story 1.6) is in the requested set are returned; the filter matches the rendered `current_state`, never a divergent stored value.

3. **Given** `GET /sessions?state=working,waitinginput,idle,unknown` (i.e. every non-`Ended` state) **When** the daemon responds **Then** the `Ended` graveyard is excluded — this is the canonical "drop the dead sessions" call a triage presenter makes. **Given** `GET /sessions?state=ended` **Then** only `Ended` sessions are returned (the inverse — a history/audit view). Because `current_state_for_read` never produces or consumes `Ended` (it only maps stale `Working`→`Idle`), the `Ended`/non-`Ended` partition is exact regardless of the read-time fallback.

4. **Given** `GET /sessions?state=<token>` with an unrecognized token (e.g. `?state=running`, `?state=foo`) **When** the daemon parses the query **Then** it returns `400 Bad Request` with body `{"error": "..."}` naming the invalid token and the accepted set — NOT a silent empty list, NOT a 500. (Strict-inbound policy, project-context §"Wire format": typos fail loudly.)

5. **Given** `GET /sessions?since=<updated_at_ms>` where `<updated_at_ms>` is an epoch-ms integer **When** the daemon queries **Then** only sessions with `updated_at > <updated_at_ms>` are returned (exclusive lower bound on the `updated_at` column — the same `updated_at` carried in each `SessionListItem`), filtered at the SQL layer; `?since=0` or an absent `since` imposes no lower bound (current behavior); a non-integer `since` yields `400`.

6. **Given** `GET /sessions?limit=<n>` where `<n>` is a positive integer **When** the daemon queries **Then** at most `<n>` rows are read from `session_projections` via a SQL `LIMIT` applied to the ordered (`updated_at DESC, source ASC, session_id ASC`), `since`-filtered query; an absent `limit` imposes no cap (current behavior); `limit=0` or a negative/non-integer `limit` yields `400`.

7. **Given** `GET /sessions?state=<csv>&limit=<n>` (state-filter combined with limit) **When** the daemon responds **Then** the SQL `LIMIT <n>` is applied to the pre-state-filter ordered row set, then the read-time `?state=` filter is applied in Rust — so the returned array MAY contain fewer than `<n>` items when some of the `<n>` fetched rows are filtered out by state. This interaction is documented (`docs/protocol.md`). `?since=`/`?limit=` are a recency filter and a row cap, **not true cursor pagination** — there is no stable exact-N page (a real cursor over `(updated_at, source, session_id)` stays deferred, see Task 9 / deferred-work.md). `?state=`, `?since=`, and `?limit=` are independently optional and compose.

8. **Given** any combination of `?state=`/`?since=`/`?limit=` **When** the response is built **Then** each returned `SessionListItem` is identical in shape and field values to what the unfiltered endpoint would have returned for that same row (the filters select *which* rows, never *how* a row is rendered); the `__daemon__` sentinel filter (`source != '__daemon__'`) is always applied underneath the new filters; corrupt-JSON rows are still skipped-with-log, not 500'd (mirrors pre-5.8 `list` discipline).

9. **Given** the protocol crate **When** Story 5.8 lands **Then** `crates/protocol/src/ws.rs` `ClientMessage::Subscribe` gains `#[serde(default)] states: Vec<String>` (an empty vec — the default when the field is absent — means unfiltered, preserving v1.0 behavior); `ClientMessage` keeps `deny_unknown_fields` (the new field is known, so old clients omitting it parse fine and a typo'd *other* key still 1008-closes); the `states` values are dumb strings parsed daemon-side (mirroring how `topic: String` is parsed by `Topic::parse`), NOT a typed `Vec<SessionCurrentState>` (which would silently map a typo to `Unknown` per its `#[serde(other)]`).

10. **Given** a WS client sends `Subscribe { topic: "state.session.*", states: ["working","waitinginput","idle"] }` **When** the daemon builds the snapshot-on-subscribe burst (`snapshot_for_topic`, Story 2.3) **Then** only sessions whose read-time-derived `current_state` is in `states` appear in the snapshot frames (here: the `Ended` graveyard is excluded); an empty/absent `states` yields the full current behavior (every matching session). The WS array is validated **per element** by `parse_states_array` (each element is one token), sharing the same case-insensitive token table (`parse_state_token`) as the REST `?state=` CSV parser (`parse_state_filter`) — a comma-bearing element like `"working,ended"` is rejected, not split. An invalid token closes the connection with a `bad message` reason via the existing invalid-topic path (NOT a silent empty snapshot). A non-empty `states` is valid **only on a `state.session.*` family topic**; a `states` filter on an event topic (which has no snapshot to scope) closes with `bad message` rather than being silently ignored (ADR 0008, pass-3 decision). The **live** event/state stream after subscription is **unchanged** — `states` scopes only the initial unsolicited snapshot burst; a session that later transitions to/from `Ended` still produces live `state.*` frames per Story 5.2's transitions-only gating.

11. **Given** the snapshot scoping in AC #10 **When** a presenter needs the full session history including `Ended` **Then** it simply omits `states` (or uses REST `GET /sessions?state=ended` for the graveyard alone) — the snapshot stays presenter-controlled and the REST surface stays the complete query (consistent with the Story 2.3 doc: "Event history is fetched via REST; the live event stream picks up at subscribe-time"). The default on both surfaces is unfiltered; nothing is filtered unless the presenter asks.

12. **Given** the documentation surface **When** Story 5.8 lands **Then** `docs/protocol.md` documents the `GET /sessions` `?state=`/`?since=`/`?limit=` params (accepted values, default-unfiltered, the `limit`+`state` interaction from AC #7, the `since`=`updated_at`-recency semantics) AND the `Subscribe.states` snapshot filter (accepted tokens, default-unfiltered, snapshot-only scope); `docs/protocol-changelog.md` gains ONE `type: schema` entry under `## v1.0 → v1.1` `(Resolves: 5.8)` covering the `ClientMessage::Subscribe.states` field (additive, `#[serde(default)]`, old presenters unaffected) AND the additive REST query params; `docs/presenter-authoring.md` gains a short note that triage presenters request `GET /sessions?state=working,waitinginput,idle` and `Subscribe` with the matching `states` rather than filtering client-side. `docs/bmad/project-context.md` is updated in the SAME PR per ADR 0008's `Affects context.md sections: Wire format, HTTP surface`.

13. **Given** a v1.0 / pre-5.8 presenter **When** it connects to a Story-5.8+ daemon and sends `Subscribe { topic }` WITHOUT `states` **Then** the daemon deserializes it (the `#[serde(default)]` fills `states` empty) and returns the full unfiltered snapshot — no decode error, no behavior change. An additive-compat test in `crates/protocol/tests/contract_protocol.rs` asserts a `Subscribe` JSON without `states` deserializes (and one with `states` round-trips). Note: `ClientMessage` is inbound/strict, so this is the deserialize-with-and-without-the-field shape, not the permissive-outbound shape the Story 5.3/5.7 tests use for `SessionState`.

14. **Given** the planning artifacts **When** Story 5.8 lands **Then** the `epics.md` §"Story 5.8" stub is replaced with the real ACs derived from this story (noting ADR 0008); `sprint-status.yaml` `5-8-server-side-session-filter` transitions `backlog → ready-for-dev` (at create-story time) with a `last_updated` breadcrumb; `deferred-work.md` line ~53 ("No pagination on `GET /sessions`") is marked **partially addressed** by 5.8 (filters + row cap kill the unbounded response; true cursor pagination stays deferred), matching the file's resolved-entry style; bean `gt-3cnt` is updated noting the filter half is resolved here and the retention sweep stays open.

15. **Given** the full workspace test suite **When** Story 5.8 lands **Then** `cargo test --workspace -- --test-threads=1`, `cargo fmt --check`, and `cargo clippy --all-targets --workspace -- -D warnings` are all green; the protocol-changelog gate (`BOWERBIRD_CHANGELOG_GATE_BASE=origin/main`) passes (the `type: schema` entry satisfies the `crates/protocol/src/ws.rs` touch); new contract tests cover: `?state=` single + multi + invalid-token-400; `?since=` lower-bound + invalid-400; `?limit=` cap + invalid-400; the `?state=`+`?limit=` fewer-than-limit interaction; unfiltered-response-unchanged regression; the `Subscribe.states` snapshot filter (a subscriber against a DB with mixed `Ended`/active rows receives snapshot frames for only the requested states, while a live transition to `Ended` post-subscribe still arrives); an invalid `states` token closes with bad-message; and the `Subscribe`-without-`states` additive-compat round-trip.

## Tasks / Subtasks

> **Approach decided: presenter-controlled filtering on both surfaces (ADR 0008).** REST gets `?state=`/`?since=`/`?limit=` query params; the WS snapshot is scoped by an optional `states` field on `ClientMessage::Subscribe`. This **modifies the wire protocol** — the protocol-changelog gate fires (Task 9 / a `type: schema` entry covers it), ADR 0008's `Affects context.md sections` requires a same-PR `project-context.md` touch (Task 7), and the additive-compat policy applies (Task 4 makes the field `#[serde(default)]`). Read the Dev Note "Why presenter-controlled (approach A), and what was rejected" before starting.

- [x] **Task 1: Shared state-filter parse + match helper** (AC: #2, #3, #4, #10)
  - [x] Add a small helper module/fn the REST handler AND the WS snapshot both call, so the `?state=` semantics are defined once. Suggested home: `crates/daemon/src/api/sessions.rs` (or a new `crate::api::filter` module if it reads cleaner). Two functions:
    - `parse_state_filter(raw: &str) -> Result<Vec<SessionCurrentState>, String>` — split `raw` on `,`, trim each token, lowercase, map to `SessionCurrentState` (`"idle"`→`Idle`, `"working"`→`Working`, `"waitinginput"`→`WaitingInput`, `"ended"`→`Ended`, `"unknown"`→`Unknown`). An empty/whitespace token or an unrecognized token returns `Err(<message naming the bad token + accepted set>)`. An empty overall string is the caller's concern (treat absent param as "no filter", not as a call to this fn).
    - `state_matches(filter: &[SessionCurrentState], derived: SessionCurrentState) -> bool` — `filter.is_empty() || filter.contains(&derived)`. **Operates on the read-derived `current_state`, never the stored one.**
  - [x] Do NOT reuse `event_kind_from_db_str`-style JSON round-tripping — that parses PascalCase wire strings; here we want case-insensitive presenter-friendly tokens. Keep the token table explicit and small (5 variants). Add a unit test for the parser (valid single, valid multi mixed-case, invalid token → Err, empty token in a list → Err).

- [x] **Task 2: `GET /sessions` query params — `?state=`/`?since=`/`?limit=`** (AC: #1, #2, #3, #4, #5, #6, #7, #8)
  - [x] Edit `crates/daemon/src/api/sessions.rs::list`. Add an axum `Query` extractor, mirroring `crates/daemon/src/api/events.rs::EventsParams` exactly (`#[derive(Debug, Deserialize)]` + `#[serde(deny_unknown_fields)]`):
    ```rust
    #[derive(Debug, Deserialize)]
    #[serde(deny_unknown_fields)]
    pub struct SessionsParams {
        #[serde(default)]
        pub state: Option<String>,   // CSV of state tokens; None = unfiltered
        #[serde(default)]
        pub since: i64,              // updated_at exclusive lower bound; 0 = no bound
        #[serde(default)]
        pub limit: Option<i64>,      // SQL LIMIT; None = no cap
    }
    ```
    Change the handler signature to `pub async fn list(State(state): State<AppState>, Query(params): Query<SessionsParams>) -> Response`. (`deny_unknown_fields` means an unknown query key 400s — consistent with the strict-inbound policy and with `EventsParams`. Verify axum returns 400, not 422, for the Query rejection; if it surfaces as 422 mirror whatever `EventsParams` already does and document it.)
  - [x] **Parse + validate up front, return 400 on bad input** (AC #4, #5, #6):
    - If `params.state` is `Some(raw)`, call `parse_state_filter(raw)`; on `Err(msg)` return `400` with `Json(json!({"error": msg}))`. On `Ok(v)` keep the `Vec<SessionCurrentState>`. `None` → empty vec (no filter).
    - If `params.limit` is `Some(n)` and `n <= 0` → `400` (`"limit must be a positive integer"`). `since < 0` → `400` (or clamp to 0 — pick 400 for consistency with limit; document).
  - [x] **SQL: add `since`/`limit` to the query** (AC #5, #6, #7). The current `SELECT_NON_SENTINEL_SESSIONS` is a `&'static str` with no params. Add filtered variants in `crates/daemon/src/db/queries.rs` (Task 3) and bind params. Push `since` (`AND updated_at > ?`) and `limit` (`LIMIT ?`) to SQL. Do **NOT** push `?state=` to SQL — it is applied in Rust on the read-derived state (see the design note). Keep the `WHERE source != '__daemon__'` sentinel filter and the `ORDER BY updated_at DESC, source ASC, session_id ASC` ordering in every variant.
  - [x] **Rust: apply `?state=` after `current_state_for_read`** (AC #2, #3, #7, #8). Inside the existing per-row loop (after `let current_state = current_state_for_read(&stored, now_ms);`), `if !state_matches(&state_filter, current_state) { continue; }` BEFORE pushing the `SessionListItem`. Everything else about the `SessionListItem` construction is unchanged (Story 5.7 fields `cwd`/`started_at` ride through).
  - [x] Confirm the unfiltered path (no params) produces the identical SQL + identical output as pre-5.8 (AC #1) — the simplest way is: when `since == 0 && limit.is_none()`, the query string equals (or is semantically identical to) the original `SELECT_NON_SENTINEL_SESSIONS`. A regression test pins this.

- [x] **Task 3: Filtered query variants in `queries.rs`** (AC: #5, #6, #7)
  - [x] Edit `crates/daemon/src/db/queries.rs`. Keep `SELECT_NON_SENTINEL_SESSIONS` (used by the unfiltered path AND by `snapshot_for_topic`). Add the `since`/`limit` clauses. Two clean options — pick one and comment it:
    - (a) Build the SQL string dynamically in `list` (base + optional `AND updated_at > ?` + optional `LIMIT ?`), binding params positionally. Keep it readable; this is a read path, not the hot ingest path.
    - (b) Add a couple of `const` variants (e.g. `SELECT_NON_SENTINEL_SESSIONS_SINCE`, `..._SINCE_LIMIT`). More constants but each is a fixed string.
    - **Recommendation: (a)** — three independent optional clauses (`since`, `limit`) combinatorially explode into too many constants; a small dynamic builder with bound params (NEVER string-interpolated values — use `?` placeholders to avoid injection) is clearer. Mirror the `params!`/positional-bind style already in the file.
  - [x] The SQL `LIMIT ?` binds an `i64`; `since` binds an `i64`. Use placeholders, bind via the `interact` closure's `query_map` params (the current call passes `[]` — it will now pass the bound params). Watch the closure's `move`/borrow of `since`/`limit`.

- [x] **Task 4: Protocol — `Subscribe.states` field + additive-compat test** (AC: #9, #13)
  - [x] Edit `crates/protocol/src/ws.rs`. Change `ClientMessage::Subscribe { topic: String }` to `Subscribe { topic: String, #[serde(default)] states: Vec<String> }`. Keep `#[serde(tag = "op", rename_all = "snake_case", deny_unknown_fields)]` on the enum unchanged — `#[serde(default)]` makes the field optional for old clients (absent → empty vec → unfiltered), while `deny_unknown_fields` still rejects a typo'd *other* key. Add a doc comment: "`states` (Story 5.8, ADR 0008): optional snapshot-scoping filter. When non-empty, the snapshot-on-subscribe burst includes only sessions whose read-time `current_state` is in this set (tokens are `SessionCurrentState` wire values, case-insensitive, parsed daemon-side like `topic`). Empty/absent = unfiltered. Scopes ONLY the snapshot; the live stream is unaffected."
  - [x] `Vec<String>` (dumb strings), NOT `Vec<SessionCurrentState>` — a typed field would silently map a typo to `Unknown` (its `#[serde(other)]`), losing the loud-reject the strict-inbound policy wants. The daemon parses the strings via `parse_state_filter` (Task 1) at the `Subscribe` arm and closes with `bad message` on an invalid token (Task 5).
  - [x] `cargo build -p protocol`. No new deps. The changelog gate WILL fire on this `protocol/src/ws.rs` touch — Task 7's `type: schema` entry satisfies it (verify in Task 9).
  - [x] **Additive-compat test** (AC #13). Edit `crates/protocol/tests/contract_protocol.rs`. Add `subscribe_without_states_deserializes_to_empty`: `{"op":"subscribe","topic":"state.session.*"}` deserializes to `ClientMessage::Subscribe` with `states == []` (the pre-5.8 wire shape still parses — `#[serde(default)]`). Add `subscribe_with_states_round_trips`: `{"op":"subscribe","topic":"...","states":["working","idle"]}` deserializes with `states == ["working","idle"]`. **Note** this is the INBOUND/strict shape (deserialize-with-and-without-the-field), NOT the permissive-outbound `SessionState` shape the Story 5.3/5.7 `additive_compat_*` tests use — `ClientMessage` carries `deny_unknown_fields`, so don't try to deserialize it with an *unknown* extra key (that SHOULD error).

- [x] **Task 5: WS — thread `states` into the snapshot path** (AC: #10, #11)
  - [x] Edit `crates/daemon/src/projection/snapshot.rs::snapshot_for_topic`. Add a `state_filter: &[SessionCurrentState]` parameter (after `now_ms`). In the per-row loop, after `let derived_current = current_state_for_read(&stored, now_ms);` (line ~131), add `if !state_matches(state_filter, derived_current) { continue; }` BEFORE pushing the `StateFrame`. Empty filter = match all (current behavior). Import `SessionCurrentState` at module scope (the `#[cfg(test)]` block already imports it; production code needs it now). Update the existing `snapshot_for_topic` callers in its own `#[cfg(test)]` tests to pass `&[]`.
  - [x] Edit `crates/daemon/src/api/ws.rs` `ClientMessage::Subscribe` arm. Destructure the new field: `ClientMessage::Subscribe { topic, states }`. Parse `states` via `parse_states_array` (per-element validation — a comma-bearing element is rejected, NOT CSV-joined; review pass-1 finding #3) — on `Err(msg)`, `close_with_bad_message(socket, &msg).await; return false;` (mirror the `Topic::parse` `Err(())` → `close_with_bad_message` path). On `Ok(filter)`, thread `&filter` into the snapshot call. **Snapshot dedup is per `(source, session_id)` key, not per topic (review pass-2 findings F1–F3):** the call is `snapshot_for_topic(&state.db.reader, &t, snapshotted_keys, now_ms, &filter)` and the handler records each delivered frame's key into the per-connection `snapshotted_keys: HashSet<(String,String)>`. An empty `states` parses to an empty filter → unfiltered snapshot, identical to today.
  - [x] **Live-stream invariant:** do NOT touch the broadcast/dispatch path (`rx.recv()` loop, `Topic::matches`). `states` scopes only the snapshot built at step [C]; a session transitioning to/from `Ended` after subscribe still delivers its live `state.*` frame (Story 5.2 emits a `State` broadcast on the genuine transition the liveness probe sets). AC #10's last sentence tests this invariant.
  - [x] The `Unsubscribe` arm does NOT gain `states` (it has no snapshot); `ClientMessage::Unsubscribe { topic }` is unchanged on the wire. It DOES prune `snapshotted_keys` of keys no longer matched by any remaining subscription (`Topic::covers_state_session`), so coverage lapses and a re-subscribe re-snapshots the drift (review pass-2 finding F2).

- [x] **Task 6: REST contract tests** (AC: #1–#8, #15)
  - [x] Edit `crates/daemon/tests/contract_daemon.rs`. Add a `story_5_8_session_filter` test module (mirror the `story_5_4_*` module style). Use the existing harness that spins a daemon + reader pool against a tempfile DB and seeds `session_projections` rows via `UPSERT_SESSION_PROJECTION` (see `snapshot.rs` tests and existing `contract_daemon.rs` session tests for the seed helper). Cover:
    - `sessions_unfiltered_unchanged` — seed mixed states; `GET /sessions` returns all non-sentinel rows in the documented order, every field intact (AC #1, #8).
    - `sessions_state_filter_single` — `?state=working` returns only `Working` (read-derived) rows (AC #2).
    - `sessions_state_filter_multi_drops_ended` — `?state=working,waitinginput,idle` excludes `Ended` rows (AC #3).
    - `sessions_state_filter_ended_only` — `?state=ended` returns only `Ended` (AC #3).
    - `sessions_state_filter_invalid_token_400` — `?state=running` → 400 with the bad token named (AC #4).
    - `sessions_since_lower_bound` — seed rows with known `updated_at`; `?since=<mid>` returns only rows with `updated_at > mid` (AC #5); `?since=notanint` → 400.
    - `sessions_limit_caps_rows` — `?limit=2` returns at most 2 rows in order (AC #6); `?limit=0` and `?limit=-1` → 400.
    - `sessions_limit_plus_state_may_return_fewer` — seed e.g. `[Ended, Ended, Working]` in `updated_at DESC` order; `?state=working&limit=2` fetches the 2 newest (both `Ended`), state-filters to 0 → empty array (proves the documented AC #7 interaction, not a bug).
    - **Read-derived correctness** — seed a stale `Working` row (stored `Working`, `last_event_at_ms` old enough that `current_state_for_read` renders `Idle`); assert `?state=idle` INCLUDES it and `?state=working` EXCLUDES it (proves the filter matches rendered state, not stored — AC #2). Reuse whatever staleness threshold `current_state_for_read` uses; check `projection::state` for the constant.
  - [x] `unwrap()` is fine in tests; tempfile DBs (not `:memory:`) for anything exercising the reader pool / WAL.

- [x] **Task 7: WS snapshot filter test** (AC: #10, #11, #15)
  - [x] Edit `crates/daemon/src/projection/snapshot.rs` `#[cfg(test)] mod tests` (it already has `fresh_db` + seed helpers and tests like `snapshot_for_topic(&reader, &Topic::StateAll, &HashSet::new(), now_ms)` — these now pass `&[]` as the new last arg, unfiltered). Add `snapshot_states_filter_excludes_unmatched`: seed a DB with active (`Working`/`WaitingInput`/`Idle`) AND `Ended` sessions; call `snapshot_for_topic(.., &[Working, WaitingInput, Idle])`; assert the frames cover only the active sessions, none `Ended`.
  - [x] Add `snapshot_empty_filter_returns_all`: same seed, `&[]` filter → all sessions (proves default-unfiltered, AC #11).
  - [x] Add `snapshot_states_filter_matches_read_derived`: a stale-`Working` row (renders `Idle`, NOT `Ended`) is INCLUDED by `&[Idle]` and EXCLUDED by `&[Working]` — proves the filter keys on the read-derived `current_state`, consistent with REST.
  - [x] The end-to-end "invalid `states` token closes with bad-message" and "live transition to `Ended` post-subscribe still arrives" halves are WS-handler behavior — cover the invalid-token close in a `contract_daemon.rs` WS test if the harness supports it (mirror however invalid-topic close is tested); if expensive, unit-test `parse_state_filter` rejection (Task 1) + assert the `Subscribe` arm routes `Err` to `close_with_bad_message`, and note the live-delivery invariant is covered by Story 2.2/5.2 tests this story does not regress.

- [x] **Task 8: Documentation** (AC: #12)
  - [x] Edit `docs/protocol.md` §`GET /sessions`: document the three query params — `?state=<csv>` (accepted tokens `idle,working,waitinginput,ended,unknown`, case-insensitive, default unfiltered, filters on the read-time `current_state`), `?since=<updated_at_ms>` (exclusive lower bound on `updated_at`; recency filter, not an opaque cursor), `?limit=<n>` (positive; SQL cap on the ordered pre-state-filter set). Document the `?state=`+`?limit=` interaction (a page may return fewer than `limit` after state filtering — paginate via `?since=` for exact-N). State that invalid values 400.
  - [x] Edit `docs/protocol.md` WS / `Subscribe` + snapshot section (the Story 2.3 snapshot-on-subscribe area): document the optional `states` field on the `subscribe` op (accepted tokens, case-insensitive, default-absent = unfiltered, scopes ONLY the snapshot burst — the live stream is unaffected); note an invalid token closes the connection (`bad message`); note full history including `Ended` is available via REST `GET /sessions` (`?state=ended` for the graveyard alone).
  - [x] Edit `docs/protocol-changelog.md`: ONE `type: schema` entry under `## v1.0 → v1.1` (after the Story 5.7 entry), `(Resolves: 5.8)`. Cover: (1) `ClientMessage::Subscribe` gains optional `states: Vec<String>` (`#[serde(default)]`; a v1.0 presenter omitting it is unaffected; scopes the snapshot-on-subscribe burst by read-time `current_state`); (2) additive `GET /sessions` `?state=`/`?since=`/`?limit=` query params (default-unfiltered preserves v1.0 behavior). Reference ADR 0008. Match the prose density of the Story 5.3/5.7 `type: schema` entries.
  - [x] Edit `docs/presenter-authoring.md`: short note — a multi-session triage presenter should request `GET /sessions?state=working,waitinginput,idle` and `Subscribe` with the matching `states`, instead of fetching everything and hiding `Ended` client-side (which deck + web both did before 5.8). Mention `Ended` is non-terminal (a filtered-out session can revive and reappear on the live stream / next unfiltered fetch).
  - [x] **`project-context.md` (ADR 0008 same-PR protocol) — REQUIRED in this PR.** This task changes the wire protocol (`ClientMessage::Subscribe`) + the HTTP surface (`/sessions` filters), triggering ADR 0008's `Affects context.md sections: Wire format, HTTP surface`. In the same PR, edit `docs/bmad/project-context.md`: §"Wire format" — note the additive inbound `Subscribe.states` field (the asymmetric `deny_unknown_fields` policy already covers inbound-strict; `states` is an additive optional inbound field); §"HTTP surface" — add the `/sessions` `?state=`/`?since=`/`?limit=` filter params where the data endpoints are listed. One line each. The CI doc-touch gate on `crates/protocol/src/*.rs` fires for the `ws.rs` change — the `protocol-changelog.md` entry satisfies it mechanically, but ADR 0008 additionally requires these context.md sections be updated, not just touched.

- [x] **Task 9: Planning-artifact bookkeeping** (AC: #14)
  - [x] Edit `docs/bmad/planning-artifacts/epics.md` §"Story 5.8: Server-side session filter" — replace the stub paragraph with the real ACs (summarize ACs #1–#12 above; note ADR 0008; keep the breadcrumb pointing at `sprint-change-proposal-2026-06-01-dogfood-triage.md` §4.2). **Do NOT renumber anything** — Story 5.7 already inserted all four stubs (5.7–5.10) and renumbered the release tail (old 5.7–5.10 → 5.11–5.14). The slot exists; only its ACs change.
  - [x] Edit `docs/bmad/implementation-artifacts/sprint-status.yaml`: `5-8-server-side-session-filter` is `backlog`; transition `backlog → ready-for-dev` at create-story time, then `→ in-progress` at dev-story start, `→ review` post dev-story, `→ done` post code-review. Add a `last_updated` comment line per transition.
  - [x] Edit `docs/bmad/implementation-artifacts/deferred-work.md`: mark the "No pagination on `GET /sessions`" entry (line ~53) **partially addressed** by 5.8 (filters + row cap; true cursor pagination still deferred). **Leave** the "No page-size limit on `GET /sessions/{id}/events`" entry (line ~52) OPEN — that is a different endpoint (`api/events.rs`) and is NOT in this story's scope (see Scope fence).
  - [x] Update bean `gt-3cnt` (`pt beans update 3cnt -d -` via stdin, preserving existing body): note the server-side filter half is resolved by Story 5.8; the `Ended` retention sweep (managed truncation) stays open as a post-V1 no-list (`no gc`) deferral.

- [x] **Task 10: Full workspace gate** (AC: #15)
  - [x] `cargo test --workspace -- --test-threads=1` (serialized per Epic 2 retro AI-3).
  - [x] `cargo fmt --check` and `cargo clippy --all-targets --workspace -- -D warnings`.
  - [x] **The protocol-changelog gate FIRES** (this story touches `crates/protocol/src/ws.rs`). Verify with `BOWERBIRD_CHANGELOG_GATE_BASE=origin/main cargo test --workspace -- protocol_changelog_gate --test-threads=1`; the Task 8 `type: schema` entry satisfies it. (If it fails, the changelog entry is missing or malformed — fix Task 8, not the gate.)

- [x] **Task 11: Smoke the filters — REST manual (live daemon), WS automated e2e** (AC: #2, #3, #5, #6, #10)
  > The REST half (`?state=`/`?since=`/`?limit=`/invalid→400) was run by hand against the live `bower.db` graveyard (see Debug Log). The WS snapshot half was NOT poked with `wscat`/`websocat` (none installed); it is covered by automated e2e against a real daemon + tungstenite client (`story_2_3_snapshot::*`, `story_2_1_ws::ws_invalid_states_token_closes_with_policy_violation`) — see the WS bullet below and the Completion Notes.
  - [x] Build release, restart the daemon (it already migrated to v3 in Story 5.7 — no new migration here). With live + ended sessions present:
    - `curl -s -H "Authorization: Bearer $(jq -r .token ~/.bowerbird/server.json)" 'http://127.0.0.1:<port>/sessions?state=working,waitinginput,idle' | jq 'length'` vs unfiltered `length` — confirm the ended count drops out.
    - `?state=ended` returns only the graveyard; `?state=bogus` returns 400; `?limit=3` caps; `?since=<recent-ms>` narrows to recent.
    - **WS snapshot smoke — covered by automated e2e, NOT manual `wscat`** (review finding #7): no `wscat`/`websocat`/node `ws` client was installed on the maintainer's machine, so the live WS poke was not run by hand. Equivalent coverage runs in CI against a real daemon + real tungstenite client: `story_2_3_snapshot::snapshot_states_filter_excludes_ended_live_transition_still_arrives` (snapshot WITH `states` excludes the `Ended` graveyard; a live transition INTO `Ended` post-subscribe still arrives), `story_2_3_snapshot::filtered_subscribe_does_not_suppress_later_wider_snapshot` (the deck's show-ended trajectory; finding #2), the existing unfiltered snapshot tests (default-unfiltered preserved), and `story_2_1_ws::ws_invalid_states_token_closes_with_policy_violation` (bogus token → 1008 close). The dogfooding success criterion (deck/web subscribe with `states`, connect is no longer a graveyard dump) is a `bowerbird-deck`/web adoption follow-up, not a daemon change in this story.
  - [x] **Out of scope** (do NOT touch): daemon supervision / start-on-login (Story 5.9), shim names-the-cause-on-daemon-down (Story 5.10), the `Ended` retention sweep (`gt-3cnt`, post-V1), `/sessions/{id}/events` page-size limit (separate deferred-work entry).

### Review Findings

- [x] [Review][Decision] **Resolved 2026-06-02 (recency-filter + cap; wording corrected, true pagination stays deferred — see Change Log).** Clarify whether `?since`/`?limit` is a recency cap or true pagination -- `docs/protocol.md:75-77` documents `?since=<updated_at_ms>` as an exclusive recency lower bound, but then says presenters needing exactly-N active sessions can "paginate via `?since=`"; `deferred-work.md:53` also marks "No pagination on `GET /sessions`" resolved. The current SQL (`updated_at > ?`, ordered by `updated_at DESC, source ASC, session_id ASC`) cannot page older rows, can skip rows tied at the `updated_at` boundary when combined with `limit`, and cannot report read-time stale `Working` -> `Idle` changes because those do not advance `updated_at`. Decide whether Story 5.8 is only a recency filter + row cap (then correct protocol/deferred-work wording), or whether it must implement true cursor pagination (for example, a tuple cursor over `updated_at/source/session_id` plus direction semantics). Evidence: `crates/daemon/src/db/queries.rs:67-74`, `docs/protocol.md:75-77`, `docs/bmad/implementation-artifacts/deferred-work.md:53`.

- [x] [Review][Patch] **Resolved 2026-06-02 (dedup keys on a `fully_snapshotted` set; new test).** Snapshot dedupe ignores `states` filter coverage [crates/daemon/src/projection/snapshot.rs:69] -- `snapshot_for_topic` short-circuits when `pre_existing` already contains `StateAll` or the exact topic. After Story 5.8, snapshot coverage depends on both topic and the subscribe-time `states` filter, but the connection only stores `HashSet<Topic>`. Example: first subscribe to `state.session.*` with `states:["ended"]` snapshots only ended rows and inserts `StateAll`; a later subscribe to the same topic with `states:["working","idle"]` returns no snapshot even though active pre-existing sessions were never sent. Fix by tracking snapshot coverage with the filter, by disabling this pre-existing short-circuit for filtered subscribes, or by otherwise proving the previous filter covers the new filter before suppressing the snapshot.

- [x] [Review][Patch] **Resolved 2026-06-02 (`parse_states_array` validates per element; comma-bearing element rejected).** Validate WS `states` as individual array tokens, not by CSV-joining [crates/daemon/src/api/ws.rs:459] -- the implementation calls `parse_state_filter(&states.join(","))`, so `states:["working,ended"]` is accepted as two valid tokens even though the WS protocol documents `states` as an array of state-token strings. This weakens the strict inbound contract and diverges from `topic` parsing, where one field value is one grammar input. Reject comma-bearing elements or add a single-token parser that REST CSV parsing can call per split token.

- [x] [Review][Patch] **Resolved 2026-06-02 (derived `Serialize`/`PartialEq`; test truly round-trips; unfiltered serialize = v1.0 shape).** `ClientMessage` still does not implement `Serialize`, and the `states` test does not actually round-trip [crates/protocol/src/ws.rs:51] -- project context says all public protocol types implement `Serialize` + `Deserialize` (`docs/bmad/project-context.md:372`), and AC #13 asks for a subscribe-with-`states` round-trip. The current diff keeps `ClientMessage` at `#[derive(Debug, Deserialize)]`; `contract_protocol.rs::subscribe_with_states_round_trips` only deserializes and inspects fields. Either derive `Serialize` on `ClientMessage` and make the test serialize back to the expected wire shape, or explicitly update the story/context if inbound client messages are an intentional exception.

- [x] [Review][Patch] **Resolved 2026-06-02 (`sessions_unknown_query_key_400` added).** Pin unknown `/sessions` query keys with a contract test [crates/daemon/tests/contract_daemon.rs:10058] -- Story 5.8 adds `#[serde(deny_unknown_fields)]` to `SessionsParams`, so `/sessions?foo=bar` should fail loudly under the strict-inbound policy. Existing 5.8 REST tests cover invalid `state`, invalid `since`, and invalid `limit`, but not an unknown query key. Add a test for `/sessions?foo=bar` so the new behavior is intentional and cannot silently regress.

- [x] [Review][Patch] **Resolved 2026-06-02 (sleeps removed; exclusion proven by frame ordering, not wall-clock).** Remove real sleeps from the new WS states-filter test [crates/daemon/tests/contract_daemon.rs:5515] -- `snapshot_states_filter_excludes_ended_live_transition_still_arrives` uses `tokio::time::sleep(Duration::from_millis(2))` to force ordering/timestamp separation, despite this story's Testing Standards saying "No real `sleep()` for synchronization." Use direct projection-row seeding with explicit `updated_at` for the snapshot setup, or otherwise make the assertion independent of wall-clock scheduling; the live transition assertion can wait on the emitted frame without sleeping first.

- [x] [Review][Patch] **Resolved 2026-06-02 (Task 11 WS bullet reworded to credit automated e2e; Change Log AC count fixed 14→15).** Correct validation records for the unrun WS manual smoke and AC count [docs/bmad/implementation-artifacts/5-8-server-side-session-filter.md] -- Task 11 is checked complete, but the Completion Notes say the required `wscat`/`websocat` WS smoke was not run and was substituted by e2e contract tests because no client was installed. Either run the specified live WS smoke (with `states`, without `states`, bogus token close, and post-subscribe live `Ended` delivery) or uncheck/reword Task 11 so the story accurately distinguishes manual smoke from automated coverage. Also fix the Change Log mismatch: it says "14 ACs satisfied" while the story and sprint status say 15 ACs.

- [x] [Review][Patch] **Resolved 2026-06-02 (5.1/5.6/5.7 status flips + architecture backlink split into their own commit, separate from the 5.8 change set).** Separate or explicitly justify unrelated story/status document churn before merging 5.8 [docs/bmad/implementation-artifacts/5-1-first-party-presenter-tool.md:3] -- the current worktree diff for a 5.8 review also changes Story 5.1 to `done`, Story 5.6 to `done`, Story 5.7 to `done`, and updates the architecture backlink for Story 5.1. Those changes may be intentional, but they are not part of Story 5.8's implementation surface and expand the review/merge blast radius. Either split them from the 5.8 change set or add an explicit note tying them to this branch's intended scope.

- [x] [Review][Decision] **Resolved 2026-06-02 pass-2 (contract: per-`(source,session_id)`-key snapshot coverage — widening re-sends only newly-uncovered rows; documented in `protocol.md` §subscribe; see Change Log F1).** Decide the intended snapshot semantics for filtered re-subscribe widening -- The Story 5.8 fix now records `fully_snapshotted` only for unfiltered subscribes, which correctly prevents a narrow filtered subscribe from suppressing a later wider snapshot. But a wider subscribe can now duplicate rows already delivered by a narrower filter: subscribe `state.session.*` with `states:["working"]`, then subscribe the same topic unfiltered; the unfiltered snapshot includes the `Working` rows again because `fully_snapshotted` is empty for the prior filtered burst. `docs/protocol.md` still says subscribing to the same topic twice is idempotent/no double-delivery, but Story 5.8 does not say whether changing `states` should emit only newly-uncovered states, emit the full requested set again, or require unsubscribe/reconnect for a fresh snapshot. Pick the contract before patching: (a) track delivered snapshot coverage by topic + state set and emit only newly-uncovered rows on widening, (b) keep full re-snapshot behavior for changed filters but document/test that duplicates are possible, or (c) reject/ignore changed-filter re-subscribes on an already-subscribed topic. Evidence: `crates/daemon/src/api/ws.rs:556-559`, `crates/daemon/src/projection/snapshot.rs:74-85`, `docs/protocol.md:216`.

- [x] [Review][Patch] **Resolved 2026-06-02 pass-2 (per-key coverage pruned on `Unsubscribe` via `Topic::covers_state_session`; e2e `unsubscribe_lapses_coverage_resubscribe_resnapshots`).** Clear or recompute full snapshot coverage on unsubscribe [crates/daemon/src/api/ws.rs:556] -- `fully_snapshotted` outlives the active subscription set. Repro: subscribe unfiltered to `state.session.*` (records `Topic::StateAll` in `fully_snapshotted`), unsubscribe from `state.session.*` (removes only `subscriptions` at `api/ws.rs:605`), let sessions change while unsubscribed, then re-subscribe unfiltered. `snapshot_for_topic` sees `Topic::StateAll` still in `pre_existing` and short-circuits to an empty snapshot, so the client misses the catch-up state for changes that occurred while unsubscribed. Fix direction: on `Unsubscribe`, remove matching full-coverage entries from `fully_snapshotted` when the active subscription coverage no longer exists, including the `StateSession`/`StateSessionCurrent` sibling cases, and add an e2e test that unsubscribes, publishes a state change, then re-subscribes and receives a fresh snapshot.

- [x] [Review][Patch] **Resolved 2026-06-02 pass-2 (per-key dedup makes identical filtered re-subscribes idempotent; e2e `identical_filtered_resubscribe_is_idempotent`).** Make identical filtered subscribes idempotent or update the contract [crates/daemon/src/api/ws.rs:531] -- Filtered subscribes never enter `fully_snapshotted`, so sending the same `Subscribe { topic: "state.session.*", states: ["ended"] }` twice on one connection re-runs `snapshot_for_topic` with an empty `pre_existing` set both times and emits duplicate snapshot frames. That violates the current `docs/protocol.md:216` promise that subscribing to the same topic twice is idempotent/no double-delivery. Fix direction: track filtered snapshot coverage using a normalized `(Topic, state_filter)` key (order-insensitive, duplicate-token-insensitive) so identical filtered subscribes short-circuit, or deliberately change the protocol/docs/tests to say filtered re-subscribes are not idempotent.

- [x] [Review][Patch] **Resolved 2026-06-02 pass-2 (ADR 0008 §31/§50, `epics.md` §5.8, story AC #7/#14/Task 9/Dev Notes/References/File List all aligned to "recency filter + cap, true cursor pagination deferred").** Align ADR/story pagination bookkeeping with the final recency-filter decision [docs/decisions/0008-server-side-session-filter.md:31] -- The previous review resolution changed the final contract to "`?since=`/`?limit=` are filters, not true pagination" and `deferred-work.md:53` now says cursor pagination over `GET /sessions` remains deferred. But ADR 0008 still says presenters can "paginate via `?since=` for exact-N" and that the deferred `/sessions` pagination entry is resolved; Story 5.8 AC #14 / Task 9 / Dev Notes still contain the same resolved-by-5.8 language. Fix direction: update ADR 0008 consequences and the story's AC/task/dev-note bookkeeping to match `docs/protocol.md:78` and `deferred-work.md:53`: Story 5.8 partially addresses the unbounded-response pain with filters and a row cap; true cursor pagination remains deferred.

- [x] [Review][Patch] **Resolved 2026-06-02 pass-2 (Completion Notes WS-parse note → `parse_states_array` per-element + per-key dedup model; Task 5 → `snapshotted_keys` + unsubscribe prune; Task 11 title → REST-manual / WS-automated-e2e; File List updated).** Refresh stale story records from the previous 5.8 review resolution [docs/bmad/implementation-artifacts/5-8-server-side-session-filter.md:280] -- The implementation now uses `parse_states_array` and rejects comma-bearing WS array elements, but the Dev Agent Record still says WS `states` are joined into CSV and fed to `parse_state_filter`. Task 5 still describes passing `subscriptions` into `snapshot_for_topic` and parsing with `parse_state_filter`, even though the patch passes `fully_snapshotted` and parses with `parse_states_array`. Task 11 is still titled "Manual smoke against the running daemon" while its WS half is explicitly automated e2e coverage, not manual `wscat`/`websocat`. Fix direction: update the story record so future reviewers see the actual code path and validation status instead of chasing already-resolved findings.

- [x] [Review][Decision] **Resolved 2026-06-03 pass-3 (contract: REJECT — a non-empty `states` on a non-`state.session.*` topic closes 1008 via the new `Topic::is_state_session_family`; an empty `states` stays valid on any topic. Chosen over the document-no-op option because the strict-inbound axiom is the inverse of Postel: a silently-discarded valid filter is exactly the silent lie the policy forbids, and it removes the indefensible valid-vs-invalid asymmetry. e2e `ws_states_on_event_topic_closes_with_policy_violation` + `ws_empty_states_on_event_topic_is_ordinary_subscribe`; ADR 0008 §Decision + `protocol.md` §subscribe updated.)** Decide `Subscribe.states` behavior on non-state topics. `api/ws.rs` parses `states` before `Topic::parse` and before `snapshot_for_topic`; `snapshot_for_topic` then returns an empty snapshot for event topics. Today `{"op":"subscribe","topic":"events.*","states":["working"]}` is accepted and the valid `states` filter is ignored, while `states:["bogus"]` closes with `bad message` even though event topics have no snapshot to scope. The story/ADR say `states` scopes the snapshot burst, but they do not say whether a non-empty `states` on an `events.*` topic is a protocol error or a validated no-op. Pick the contract before patching: (a) reject non-empty `states` unless `topic` is a `state.session.*` family topic, or (b) explicitly document and test the current behavior: valid filters are accepted/no-op on event topics, invalid filters still 1008-close under strict inbound validation. Evidence: `crates/daemon/src/api/ws.rs:463-542`, `crates/daemon/src/projection/snapshot.rs:62-67`, `docs/protocol.md:226`, ADR 0008 §Decision.

- [x] [Review][Patch] **Resolved 2026-06-03 pass-3 (live State dispatch now records its `(source, session_id)` key into `snapshotted_keys` in BOTH the main `rx.recv()` arm and `drain_backlog_under_state`; e2e `live_state_frame_recorded_in_coverage_no_duplicate_on_resubscribe`).** Record live-delivered State frames in snapshot coverage before re-subscribes. `snapshotted_keys` is only updated while emitting snapshot frames, so a session delivered live after an empty or filtered snapshot is still considered "not covered" by the next subscribe. Repro: connect to an empty daemon, `Subscribe { topic:"state.session.*" }` (empty snapshot), publish a new session so the client receives a live `StateFrame`, then send the same subscribe again; `snapshot_for_topic` sees an empty `snapshotted_keys` entry for that session and sends a duplicate snapshot. The same gap exists in `drain_backlog_under_state`: a queued live `State` drained before a subscribe can be sent under the old subscription set, but its key is not recorded before the subsequent snapshot read. This violates the pass-2 no-double-delivery/catch-up model in `docs/protocol.md` because a live stream that kept a session current should prevent an immediate re-snapshot of the same row. Fix direction: make successful State dispatch return or record the delivered `(source, session_id)` key, update `snapshotted_keys` after successful wire sends in both the main `rx.recv()` path and the subscribe/unsubscribe drain path, and add e2e coverage for "empty snapshot -> live state -> identical re-subscribe emits no duplicate" plus a drain-path variant if feasible. Evidence: `crates/daemon/src/api/ws.rs:332-340`, `crates/daemon/src/api/ws.rs:657-662`, `crates/daemon/src/api/ws.rs:565-567`, `crates/daemon/src/api/ws.rs:920-952`, `crates/daemon/src/projection/snapshot.rs:109`, `docs/protocol.md:218-224`.

- [x] [Review][Patch] **Resolved 2026-06-03 pass-3 (code was already correct via `Topic::covers_state_session`; reworded `protocol.md` §subscribe so coverage lapses only when no remaining active state subscription covers the session; e2e `overlapping_subscription_unsubscribe_does_not_lapse_covered_session`).** Tighten unsubscribe snapshot-coverage docs to match remaining subscriptions. The implementation removes the unsubscribed topic and then retains snapshotted keys still covered by any remaining active state subscription via `Topic::covers_state_session`, but `docs/protocol.md` says "Coverage lapses on unsubscribe" / "once you unsubscribe a topic" without the remaining-subscription caveat. That wording is too broad for overlapping subscriptions: e.g. `state.session.*` plus `state.session.sess-A`, then unsubscribe only the wildcard; coverage for `sess-A` should not lapse while the specific subscription still delivers its live state. Fix direction: reword the protocol docs to say coverage for a `(source, session_id)` lapses only when no remaining active state subscription covers that session, and add an overlapping-subscription e2e test that unsubscribes one of two covering topics, mutates the session, then verifies re-subscribe behavior. Evidence: `crates/daemon/src/api/ws.rs:608-618`, `crates/daemon/src/broadcast/event.rs:140-154`, `docs/protocol.md:224`.

- [x] [Review][Patch] **Resolved 2026-06-03 pass-3 (`?limit=notanint`→400 added to `sessions_limit_caps_rows`; new `sessions_state_filter_empty_and_trailing_token_400` pins `?state=` and `?state=working,` → 400 with an `{error}` body at the HTTP layer).** Complete REST invalid-query contract coverage. AC #6 and `docs/protocol.md` require non-integer `limit` to return `400`, but the REST contract test only covers `limit=0` and `limit=-1`; `since=notanint` is covered, `limit=notanint` is not. Also, `filter.rs` unit-tests empty/trailing state tokens, but the HTTP surface only pins `state=running`; the handler-level behavior for `?state=`, `?state=working,`, and whitespace-only state values is not pinned. Fix direction: add `GET /sessions?limit=notanint` -> `400 Bad Request`, and at least one HTTP contract assertion for an empty/trailing state token returning `400` with an error body naming the accepted set or bad token. Evidence: `docs/bmad/implementation-artifacts/5-8-server-side-session-filter.md:35`, `docs/protocol.md:76`, `crates/daemon/tests/contract_daemon.rs:10428-10439`, `crates/daemon/src/api/filter.rs:121-131`.

- [x] [Review][Patch] **Resolved 2026-06-03 pass-3 (new e2e `states_filter_on_specific_session_topic`: `state.session.sess-A` + matching `states:["working"]` snapshots the row, + non-matching `states:["ended"]` snapshots nothing — two connections so the negative has a fresh coverage set).** Add coverage for `states` on specific state topics. The WS e2e tests exercise `Subscribe.states` with `state.session.*`, and the snapshot unit tests exercise `Topic::StateSession`/`StateSessionCurrent` without a non-empty filter. The public surface allows the same `states` field on all `Subscribe` messages, and AC #10 describes snapshot scoping for the snapshot path generally, so specific state topics should be pinned too. Fix direction: add at least one contract test for `state.session.<id>` or `state.session.<id>.current_state` with a matching `states` filter and a non-matching `states` filter, asserting the snapshot appears only when the read-derived state matches. Evidence: `crates/daemon/tests/contract_daemon.rs:5492-5869`, `crates/daemon/src/projection/snapshot.rs:270-368`, `crates/daemon/src/projection/snapshot.rs:692-850`, `docs/protocol.md:226`.

- [x] [Review][Patch] **Resolved 2026-06-03 pass-3 (AC #10, Project Structure Notes, References, and ADR 0008 §Decision/§Alternatives now say WS uses per-element `parse_states_array` sharing the `parse_state_token` table with REST; comma-bearing elements rejected; the REST-side `parse_state_filter` references are correct and left as-is).** Refresh stale WS parser wording in story/ADR records. The implementation now parses WS `states` with `parse_states_array` and validates each array element as one token, sharing `parse_state_token` with REST but not feeding the WS array to `parse_state_filter`. Several records still say the WS path uses "the same `parse_state_filter`" as REST, which sends future reviewers toward a bug that was already fixed in pass 1. Fix direction: update AC #10, Project Structure Notes, Reference bullets, and ADR 0008 to say WS uses `parse_states_array` / per-element validation with the same token table as REST; mention comma-bearing array elements are rejected. Evidence: `crates/daemon/src/api/ws.rs:474`, `crates/daemon/src/api/filter.rs:55-65`, `docs/bmad/implementation-artifacts/5-8-server-side-session-filter.md:43`, `docs/bmad/implementation-artifacts/5-8-server-side-session-filter.md:229`, `docs/bmad/implementation-artifacts/5-8-server-side-session-filter.md:268`, `docs/decisions/0008-server-side-session-filter.md:32`, `docs/decisions/0008-server-side-session-filter.md:39`.

- [x] [Review][Patch] **Resolved 2026-06-03 pass-3 (reworded `protocol.md` Topic Grammar rule to "One string topic per Subscribe" — constrains the `topic` field only, with an explicit note that the sibling `states` field is an array).** Narrow protocol wording that now conflicts with `states` arrays. `docs/protocol.md` Topic Grammar says "Comma-lists, arrays, and multiple `topic` fields are all malformed." After Story 5.8, arrays are valid for `states`; the intended rule is that `topic` itself is a single string and cannot be an array/list/batch. Fix direction: reword the rule to "One string topic per Subscribe; comma-lists or arrays in the `topic` field, batched topics, and multiple `topic` fields are malformed." Evidence: `docs/protocol.md:356-368`, `docs/protocol.md:212-226`.

- [x] [Review][Patch] **Resolved 2026-06-03 pass-4 (both broadcast-lag paths — the main `rx.recv()` arm and `drain_backlog_under_state` — now `snapshotted_keys.clear()` once lag is observed with a non-empty subscription set: `Lagged(n)` reports only a count, never the evicted envelopes' identities, so we conservatively invalidate ALL coverage rather than guess which covered sessions were in the gap. A re-subscribe after a `dropped` frame re-snapshots, letting a state-only subscriber recover. New e2e `story_2_4_dropped::lag_invalidates_snapshot_coverage_resubscribe_resnapshots`; `protocol.md` §subscribe documents the lag-invalidation.) Pass 4: Invalidate snapshot coverage after broadcast lag, or a state subscriber can re-subscribe and still stay stale.** `snapshotted_keys` now suppresses future snapshots by `(source, session_id)` in `crates/daemon/src/projection/snapshot.rs:100-107`, and it is pruned on `Unsubscribe` in `crates/daemon/src/api/ws.rs:651-653`. But both lag paths (`RecvError::Lagged` in `api/ws.rs:356-380` and `TryRecvError::Lagged` in `api/ws.rs:714-740`) emit/coalesce `DroppedFrame` without invalidating any state snapshot coverage. Repro shape: subscribe `state.session.*`, receive a snapshot for `sess-A` (key recorded), fall behind while `sess-A` transitions, receive `dropped`, then send the same subscribe again to catch up. Because the key is still in `snapshotted_keys`, `snapshot_for_topic` skips `sess-A`, so the re-subscribe cannot recover the missed state on the same connection. This conflicts with the snapshot-as-catch-up model introduced by pass 2 and with the fact that state-only subscribers cannot recover missed state via `/sessions/{id}/events?since=<last_event_id>` alone. Fix direction: when lag is observed while active state subscriptions exist, conservatively invalidate snapshot coverage for sessions covered by those active state topics (or clear all `snapshotted_keys` if the per-session set is not cheaply knowable), then add a regression test that snapshots `sess-A`, forces lag across a `sess-A` state transition, re-subscribes, and receives a fresh `StateFrame`.

- [x] [Review][Patch] **Resolved 2026-06-03 pass-4 (`protocol.md` §subscribe reworded: coverage now described as the rows the connection already holds current — delivered by a snapshot burst OR a live `state` frame while covered — and never re-snapshotted; the lag-invalidation note from F1 is consistent with this model.) Pass 4: Protocol docs still say snapshot coverage only tracks snapshot-delivered rows, but pass 3 records live State frames too.** The implementation records live-delivered `BroadcastEnvelope::State` keys into `snapshotted_keys` in both the main receive loop (`crates/daemon/src/api/ws.rs:333-350`) and `drain_backlog_under_state` (`crates/daemon/src/api/ws.rs:693-707`), and the e2e `live_state_frame_recorded_in_coverage_no_duplicate_on_resubscribe` pins the behavior. `docs/protocol.md:218` still says the daemon tracks rows it has already "snapshot-delivered" on the connection. That understates the contract: once a session is delivered live while still covered by an active state subscription, a re-subscribe will not emit another snapshot for it. Fix direction: update `protocol.md` §subscribe to describe coverage as rows already delivered by snapshot OR live `state` frames while covered, and keep the unsubscribe-lapse wording aligned with that model.

- [x] [Review][Patch] **Resolved 2026-06-03 pass-4 (`docs/decisions/0008-server-side-session-filter.md:49` now reads "stable exact-N pages still require deferred cursor pagination (`?since=` is a recency bound, not a forward/backward cursor)"; the story's Dev Notes "Files this story touches" bookkeeping line changed from "mark the `/sessions` pagination entry resolved" to "partially addressed — true cursor pagination stays deferred", aligning with AC #7/#14, `protocol.md`, and `deferred-work.md:53`.) Pass 4: Remove stale exact-N pagination/resolved wording that survived the pass-2 cleanup.** The final contract says `?since=` is an exclusive `updated_at` recency bound and `?limit=` is a row cap, not true pagination. Most records now say that, but `docs/decisions/0008-server-side-session-filter.md:49` still says "a presenter needing exactly-N pages via `?since=`," and this story's Project Structure Notes still says `deferred-work.md` should "mark the `/sessions` pagination entry resolved" (`docs/bmad/implementation-artifacts/5-8-server-side-session-filter.md:233`). Those conflict with AC #7/#14, `docs/protocol.md:78`, and `deferred-work.md:53`, all of which say true cursor pagination remains deferred. Fix direction: replace the ADR sentence with "stable exact-N pages still require deferred cursor pagination," and change the story bookkeeping note from "resolved" to "partially addressed" so future agents do not re-close the cursor-pagination item.

- [x] [Review][Patch] **Resolved 2026-06-03 pass-4 (`sprint-status.yaml`'s active section now has exactly ONE `last_updated:` key — the three duplicates collapsed (full per-pass history stays in the commented breadcrumb block, with two new pass-4 lines appended), and a comment notes the single-active-key invariant. `development_status.5-8-server-side-session-filter` stays `review`, in lockstep with the story status after this pass.) Pass 4: Consolidate sprint-status active `last_updated` records so YAML consumers do not see stale 5.8 status.** `docs/bmad/implementation-artifacts/sprint-status.yaml` has a long commented breadcrumb block that correctly records the pass-3 resolution, but the active YAML section still has duplicate `last_updated:` keys at lines ~90-93. The final active value says Story 5.8 moved to `in-progress` for pass-3 unresolved findings, while `development_status.5-8-server-side-session-filter` says `review` and the comment block says pass 3 was resolved. YAML parsers keep only one duplicate key (typically the last), so consumers may report a stale review state even when the story table says otherwise. Fix direction: keep historical breadcrumbs commented, leave exactly one active `last_updated:` key, and set it to the current Story 5.8 state when this pass's findings are documented/resolved; keep `development_status` in lockstep with the story status.

- [x] [Review][Patch] **Resolved 2026-06-03 pass-4 (the 5.8 `protocol-changelog.md` entry now names the strict-reject edges: per-element WS validation (a comma-bearing `["working,ended"]` element → 1008), non-`state.session.*`-topic `states` → 1008, and unknown `/sessions` query keys (`deny_unknown_fields`) → 400. `protocol.md` §`GET /sessions` gains an "Unknown query keys → 400" bullet. Both still state the field/params are additive when omitted.) Pass 4: Changelog/protocol docs omit strict 5.8 rejection cases introduced after the original additive entry.** The final code rejects several strict-inbound cases beyond a bad state token: `SessionsParams` uses `#[serde(deny_unknown_fields)]` and `sessions_unknown_query_key_400` pins `/sessions?foo=bar` as `400`; `parse_states_array` rejects a comma-bearing WS array element such as `["working,ended"]`; and `api/ws.rs` rejects non-empty `states` on non-`state.session.*` topics. `docs/protocol.md` documents the WS non-state-topic reject, but the `/sessions` section only mentions invalid known values, and `docs/protocol-changelog.md:55` only says invalid values/token errors -> `400`/1008 without naming unknown query keys, per-element WS validation, or non-state-topic `states` rejection. Fix direction: update the 5.8 changelog entry and `docs/protocol.md` `/sessions` notes to make these loud-reject edges explicit, while still saying the field/query params are additive when omitted.

## Dev Notes

### Why presenter-controlled (approach A), and what was rejected

The design decision is recorded in **ADR 0008** — read it. The short version: filter on **both** surfaces by a **presenter-supplied predicate** (the read-time `current_state`), defaulting to unfiltered so v1.0 behavior is preserved everywhere. REST: `?state=`/`?since=`/`?limit=` query params. WS: an optional `states` field on `ClientMessage::Subscribe` that scopes the snapshot burst. The daemon never decides on its own what is "worth" sending — `states` scopes the snapshot the same way `topic` scopes which sessions you get live. That keeps the substrate a pure fact-filter (Axiom 1) and keeps both surfaces' defaults identical ("nothing filters unless you ask").

**Cost of this choice (handle it, don't be surprised by it):** it modifies the wire protocol (`ClientMessage::Subscribe`), so the changelog gate fires (Task 10), ADR 0008's `Affects context.md sections` requires a same-PR `project-context.md` touch (Task 8), and there's a forward-compat edge — a *newer* presenter sending `states` to an *older* daemon is rejected (`deny_unknown_fields`, WS close 1008). That's the unusual "client ahead of daemon" direction and is acceptable.

**The rejected alternative (approach B):** a fixed daemon-side default where `snapshot_for_topic` always drops `Ended`, no protocol change, no ADR. It was close — lighter, matches the proposal's stated file footprint, and both current presenters would be happy with it. Rejected because it has the daemon *decide* relevance (a mild Axiom-1 smell), *silently changes* the WS snapshot default (breaking the "nothing filters unless asked" symmetry the REST half keeps), and removes a presenter's ability to get the graveyard on the snapshot at all. The maintainer chose the axiom-cleanliness and symmetry of A and accepted the wire-change + ADR cost for a protocol-stability release. ADR 0008 §Alternatives records the full reasoning. **Do not implement B.**

ADR numbering note: this story takes **ADR 0008** (0007 is reserved for Story 5.9 supervision and authored later; re-assigning it is higher churn). Cookbook Story 5.12, previously told "next free," takes 0009.

### Why `?state=` filters in Rust on the read-derived state, not in SQL

The `SessionListItem.current_state` contract (`crates/protocol/src/rest.rs`) is explicit: it is the **read-time** value, after `current_state_for_read`'s stale-`Working`→`Idle` fallback (Story 1.6), NOT the raw stored value. If `?state=` filtered the stored JSON in SQL (`json_extract(state,'$.current_state')`), a stale-`Working` row would match `?state=working` while the response renders its `current_state` as `Idle` — the filter would contradict the rendered field. That is a confusing lie. Filtering in Rust, right after `current_state_for_read` is computed (it already is, in both `list` and `snapshot_for_topic`), guarantees the filter matches exactly what the presenter sees.

Cost: `?state=` can't reduce the DB row count, so it doesn't shrink the SQL scan — only `?since=`/`?limit=` do (those are pushed to SQL). At v0.1.0 scale (hundreds of sessions, ~1 MB max — see project-context "Projection growth is slow, one row per session, ~1MB over months") this is fine; the actual dogfooding pain was the unbounded *response* and the connect-time *burst*, both of which Rust-side filtering fixes (smaller response array; scoped snapshot). The `?state=`+`?limit=` interaction (AC #7: limit caps pre-filter, so a page may return < limit) is the one wart, and it's documented. `Ended` (the real use case) is exact either way because `current_state_for_read` never produces or consumes `Ended` — it only maps stale `Working`→`Idle`.

### `?since=` is a recency filter on `updated_at`, not an event cursor

Unlike `/sessions/{id}/events?since=<event_id>` (a monotonic event-id cursor, ASC order), sessions have no per-session monotonic cursor. `?since=` here is an **exclusive lower bound on the `updated_at` column** (`WHERE updated_at > ?`), the same `updated_at` each `SessionListItem` carries. Semantics: "sessions touched since this epoch-ms." With the existing `ORDER BY updated_at DESC` a presenter polling "what changed since my last poll" passes the max `updated_at` it has seen. Default `0` = no bound (all real rows have `updated_at > 0`). This **partially** addresses the deferred-work "No pagination on `GET /sessions`" intent (line ~53) — it kills the unbounded response without inventing an opaque token, but it is a recency filter, not a cursor: it cannot page older rows, can split rows tied on the boundary `updated_at` under `?limit=`, and won't surface a read-time `Working`→`Idle` change (which doesn't bump `updated_at`). True cursor pagination stays deferred.

### This is the sibling of Story 5.7 — same files, lighter touch

Story 5.7 (done) is the immediate predecessor and touched nearly every file you will: `api/sessions.rs` (it added `cwd`/`started_at` to the `SessionListItem` push and `SessionDetail.state` — see lines ~89–99 and ~170–177), `db/queries.rs` (it extended `INSERT_EVENT` and the per-event SELECTs), `projection/snapshot.rs` (it threaded `cwd`/`started_at` into the `StateFrame` push, line ~135–142). Read 5.7 (`5-7-session-cwd-on-the-wire.md`) for the established patterns and its Dev Agent Record. Key 5.7 lessons that apply:
- **`api/events.rs::EventsParams`** (`#[serde(deny_unknown_fields)]` + `#[serde(default)]`, used via `Query(params)`) is the exact pattern to mirror for `SessionsParams`. Story 5.7 did not add query params, but the events endpoint already proves the shape.
- **The list/snapshot pair share `SELECT_NON_SENTINEL_SESSIONS` and the corrupt-JSON-skip discipline** — keep them in lockstep. `snapshot.rs` line ~104 comment explicitly says "mirrors `api/sessions.rs::list` discipline."
- **Manual `params!`/SQL sites are not compiler-enforced** — 5.7's Dev Agent Record records a sentinel-writer regression (`INSERT_EVENT` got an 8th column but a hand-built `params!` site wasn't updated, crashing at smoke). You're adding `?` placeholders + bound params to a `query_map` that currently passes `[]`; double-check the bind count matches the placeholder count, and smoke it (Task 10).
- **clippy `type_complexity`** bit 5.7 on a wide row tuple — if you build a dynamic SQL string with a `Vec<Box<dyn ToSql>>` params vector, keep it clippy-clean.

### Files this story touches

**Source (4–5 files):** `crates/protocol/src/ws.rs` (`Subscribe.states` field), `crates/daemon/src/api/sessions.rs` (`SessionsParams` + `list` filtering + the shared `parse_state_filter`/`state_matches` helper, or a small new `api/filter.rs`), `crates/daemon/src/db/queries.rs` (since/limit query variants or dynamic builder), `crates/daemon/src/projection/snapshot.rs` (`state_filter` param), `crates/daemon/src/api/ws.rs` (`Subscribe` arm parses `states` + threads into `snapshot_for_topic`). **No `crates/shim/**`, no `crates/adapter-claude/**`, no migration** (the `events`/`session_projections` schema is unchanged — this is a query/transport-shape story, not a data-shape story).

**Tests (2 files):** `crates/daemon/tests/contract_daemon.rs` (`story_5_8_session_filter` module — REST + WS invalid-token close), `crates/daemon/src/projection/snapshot.rs` `#[cfg(test)]` (snapshot `states` filter), `crates/protocol/tests/contract_protocol.rs` (`Subscribe` additive-compat).

**Docs (4 files):** `docs/protocol.md`, `docs/protocol-changelog.md` (`type: schema`), `docs/presenter-authoring.md`, `docs/bmad/project-context.md` (ADR 0008 `Affects context.md sections` touch).

**Decisions (1 file, authored at create-story time):** `docs/decisions/0008-server-side-session-filter.md` (Accepted 2026-06-02).

**Planning artifacts (3 files):** `epics.md` (replace §5.8 stub ACs — NO renumber), `sprint-status.yaml` (status transitions), `deferred-work.md` (mark the `/sessions` pagination entry **partially addressed** — filters + row cap; true cursor pagination stays deferred). Plus bean `gt-3cnt`.

**Files explicitly NOT touched:** `crates/daemon/src/db/migrations.rs` (no schema change), `crates/daemon/src/api/events.rs` (`/events` page-size limit stays deferred), `crates/shim/**`, `crates/adapter-claude/**`, the `current_state` arms of `projection/state.rs` (filtering reads `current_state`, never changes the state machine).

### Project Structure Notes

- `crates/daemon/src/api/sessions.rs` — UPDATE — add `SessionsParams` (`Query` extractor mirroring `EventsParams`), validate→400, push `since`/`limit` to SQL, apply `?state=` in Rust after `current_state_for_read`.
- `crates/daemon/src/db/queries.rs` — UPDATE — `since`/`limit` clauses (dynamic builder recommended; keep `SELECT_NON_SENTINEL_SESSIONS` for the unfiltered + snapshot paths).
- `crates/protocol/src/ws.rs` — UPDATE — `ClientMessage::Subscribe` gains `#[serde(default)] states: Vec<String>`.
- `crates/daemon/src/projection/snapshot.rs` — UPDATE — `snapshot_for_topic` gains a `state_filter: &[SessionCurrentState]` param; `state_matches` skip in the per-row loop; bring `SessionCurrentState` into module scope.
- `crates/daemon/src/api/ws.rs` — UPDATE — `Subscribe` arm destructures `states`, parses per-element via `parse_states_array` (bad token → `close_with_bad_message`), rejects a non-empty `states` on a non-`state.session.*` topic via `Topic::is_state_session_family` (pass-3), threads `&filter` into `snapshot_for_topic`.
- `crates/daemon/src/api/sessions.rs` (or new `api/filter.rs`) — `parse_state_filter` + `state_matches` shared helpers.
- `crates/daemon/tests/contract_daemon.rs` — UPDATE — `story_5_8_session_filter` module.
- `crates/protocol/tests/contract_protocol.rs` — UPDATE — `Subscribe` additive-compat (with/without `states`).
- `docs/protocol.md`, `docs/protocol-changelog.md` (`type: schema`), `docs/presenter-authoring.md`, `docs/bmad/project-context.md` (ADR 0008 Affects-sections) — UPDATE.
- `docs/decisions/0008-server-side-session-filter.md` — DONE (authored 2026-06-02; Status Accepted).
- `docs/bmad/planning-artifacts/epics.md` — UPDATE — replace §5.8 stub ACs (no renumber).
- `docs/bmad/implementation-artifacts/sprint-status.yaml`, `deferred-work.md` — UPDATE.

### Testing Standards

Per `docs/bmad/project-context.md` §"Required contract tests" + §"Deterministic test discipline":
- Tempfile DBs (not `:memory:`) for reader-pool/WAL paths; `unwrap()` is fine in tests.
- No real `sleep()` for synchronization — the stale-`Working`→`Idle` test seeds an old `last_event_at_ms` directly rather than sleeping.
- The unfiltered-response-unchanged test (AC #1) is the regression canary that the filters are additive.
- `--test-threads=1` for the full workspace run (Epic 2 retro AI-3).

### What is explicitly NOT in this story (scope fence)

- **The `Ended` retention sweep / row eviction** — managed truncation, no-list "no `bowerbird gc`" cut, post-V1, `gt-3cnt`. This story makes `Ended` filterable, never deleted.
- **`/sessions/{id}/events` page-size limit** — a DIFFERENT endpoint (`api/events.rs`), its own deferred-work entry (line ~52); stays deferred. Only `GET /sessions` (the list) gets pagination here.
- **Daemon supervision / start-on-login** (Story 5.9, +ADR 0007), **shim names the daemon-down cause** (Story 5.10) — separate dogfood-triage stories.
- **A `?source=` filter / multi-source disambiguation** — separate deferred-work item; V1 has one source (`claude`).
- **Pushing `?state=` to SQL via `json_extract`** — rejected (see Dev Note / ADR 0008: would contradict the read-time `current_state` contract).
- **A fixed daemon-side snapshot default (approach B)** — rejected in favor of the presenter-supplied `states` filter (see Dev Note + ADR 0008 §Alternatives). Do not implement B.
- **Scoping the *live* stream by `states`** — `states` scopes only the snapshot burst; live `state.*`/`events.*` delivery is unchanged. Live-stream filtering is an explicit ADR 0008 "revisit when" item, not this story.

### References

- `docs/decisions/0008-server-side-session-filter.md` — **the decision record** (Accepted 2026-06-02). Why presenter-controlled filtering on both surfaces; the rejected approach-B and json_extract alternatives; the `Vec<String>`-not-`Vec<SessionCurrentState>` choice; the `gt-3cnt` retention split; the ADR-numbering note. Read first.
- `docs/bmad/planning-artifacts/sprint-change-proposal-2026-06-01-dogfood-triage.md` — the originating spec. §4.2 (the change list: `?state=`/`?since=`/`?limit=` on `api/sessions.rs::list`, filtered `SELECT_NON_SENTINEL_SESSIONS` variants, `api/ws.rs` snapshot scoping, folds the `/sessions` pagination deferred item, partially resolves `gt-3cnt`), §3 (rationale: why filter-only not a retention sweep), §6 (the retention sweep stays deferred). NOTE §4.2 said "no ADR" assuming REST-only; the maintainer's presenter-controlled WS choice modifies the wire protocol, hence ADR 0008.
- `docs/dogfooding-feedback.md` — 2026-06-01 "presenters can only triage on what the wire carries" (Finding 5: `Ended` never evicts, the full history dumped via REST and the snapshot burst). The trigger.
- `docs/bmad/implementation-artifacts/5-7-session-cwd-on-the-wire.md` — **the immediate predecessor (done)**; same files, the `SessionListItem`/`SessionDetail`/`snapshot` patterns, and the Dev Agent Record sentinel-writer + clippy lessons.
- `docs/decisions/0004-daemon-observed-session-liveness.md` — `Ended` is daemon-observed and **non-terminal** (`claude --resume` revives it); why the daemon can't delete-on-death (the reason a *filter*, not eviction, is the fix).
- `docs/bmad/project-context.md` — Axiom 1 (substrate observes, does not interpret — `Ended` is a mechanical fact; "active" is the presenter's word, which is why `?state=` takes literal state tokens), §"HTTP surface" (the `/sessions` data endpoints), §"Wire format" (strict-inbound `deny_unknown_fields` — typo'd params 400; gets a same-PR touch per ADR 0008), §"ADR triggers" (the `Subscribe.states` wire-protocol change is what triggered ADR 0008).
- `crates/daemon/src/api/sessions.rs:24–103` — `list`; the per-row loop where `current_state_for_read` is computed and `SessionListItem` is pushed (where `?state=` filtering slots in).
- `crates/daemon/src/api/events.rs:23–35` — `EventsParams` + `Query(params)` — the exact extractor pattern to mirror for `SessionsParams`.
- `crates/daemon/src/db/queries.rs:40–43` — `SELECT_NON_SENTINEL_SESSIONS` (the base query; `since`/`limit` clauses attach here; keep it for the unfiltered + snapshot paths).
- `crates/daemon/src/projection/snapshot.rs:39–146` — `snapshot_for_topic` signature (gains `state_filter`) + per-row loop; `current_state_for_read` at ~131, `StateFrame` push at ~132 (where the `state_matches` skip slots in). Its `#[cfg(test)]` callers pass `&[]`.
- `crates/daemon/src/api/ws.rs` — the `ClientMessage::Subscribe { topic, states }` arm: destructure `states`, parse per-element via `parse_states_array`, route `Err` to `close_with_bad_message` (mirror the `Topic::parse` `Err(())` path); reject a non-empty `states` on a non-`state.session.*` topic (pass-3, `Topic::is_state_session_family`); thread `&filter` into the `snapshot_for_topic(.., now_ms, &filter)` call.
- `crates/protocol/src/state.rs:17–47` — `SessionCurrentState` (`Idle`/`Working`/`WaitingInput`/`Ended`/`Unknown`) — the `?state=`/`states` token set; `SessionState`.
- `crates/protocol/src/rest.rs:34–55` — `SessionListItem`, with the explicit "`current_state` is the **read-time** projection" contract that mandates Rust-side `?state=` filtering.
- `crates/protocol/src/ws.rs:51–56` — `ClientMessage` (`#[serde(tag="op", rename_all="snake_case", deny_unknown_fields)]`); `Subscribe { topic }` gains `#[serde(default)] states: Vec<String>`. The doc-comment block at lines 35–50 explains the strict-inbound policy — the additive optional field is consistent with it.
- `crates/protocol/tests/contract_protocol.rs` — the `additive_compat_*` family (Story 5.3/5.7, permissive-outbound `SessionState`); the new `Subscribe` test is the inbound/strict mirror (deserialize with/without `states`).
- `crates/daemon/src/projection/state.rs::current_state_for_read` — the stale-`Working`→`Idle` read fallback + its staleness threshold constant (seed the threshold in the read-derived-correctness test).
- `docs/bmad/implementation-artifacts/deferred-work.md:52–53` — the two pagination entries: line 53 (`/sessions`) is *partially addressed* here (filters + row cap; true cursor pagination still deferred); line 52 (`/sessions/{id}/events`) stays deferred.

## Dev Agent Record

### Agent Model Used

claude-opus-4-8[1m] (Opus 4.8, 1M context) — bmad-dev-story workflow.

### Debug Log References

- Full workspace gate: `cargo test --workspace -- --test-threads=1` → **537 passed** (was ~509 pre-5.8; +28 from the new filter/snapshot/protocol tests). `cargo fmt --check` clean, `cargo clippy --all-targets --workspace -- -D warnings` → no issues.
- Protocol-changelog gate: `BOWERBIRD_CHANGELOG_GATE_BASE=origin/main cargo test --test protocol_changelog_gate` → pass (the `type: schema` entry satisfies the `crates/protocol/src/ws.rs` touch).
- Manual smoke (Task 11) against the live daemon on the real `bower.db` (174 sessions: 156 `Ended` + 18 active): unfiltered `174` → `?state=working,waitinginput,idle` `18` (graveyard dropped) → `?state=ended` `156`; `?state=bogus` → `400` with the token-naming error body; `?limit=3` → `3`; `?limit=0`/`?since=notanint` → `400`; `?since=<median>` → `86` (all `updated_at > median`); `?state=working&limit=5` → `1`; `?limit=5` ordered `updated_at DESC`.
- **Pass-4 verification (2026-06-03):** `cargo fmt --check` clean; `cargo clippy --all-targets --workspace -- -D warnings` clean; protocol-changelog gate green. The full `contract_daemon` binary run alone (`cargo test -p bowerbird-daemon --test contract_daemon -- --test-threads=1 --skip status_returns_none_last_event_when_only_sentinels`) → **184 passed, 0 failed, 3/3 consecutive** with the new F1 test included. F1 fix correctness independently verified by daemon tracing (lag clears `snapshotted_keys`; re-subscribe re-snapshots sess-A and emits it) and by a negative check (disabling both `clear()` sites → the F1 test fails).
- **Two test-harness issues surfaced (NOT 5.8 regressions), seeded to the test-isolation investigation:** (1) `story_1_7_rest::status_returns_none_last_event_when_only_sentinels` hangs intermittently (~1 in 5) **even run in complete isolation** — the documented "intermittent workspace-test hang" (sprint-status 2026-05-28, Story 5.3); a REST `/status` test with zero overlap with 5.8. (2) The new F1 e2e (`lag_invalidates_snapshot_coverage_resubscribe_resnapshots`) is robust when the `contract_daemon` binary runs alone but flakes under `cargo test --workspace` (fails 3/3 there) — independent of runtime flavor (`current_thread` and `multi_thread` both flake under `--workspace`, both pass alone). Reproduction + analysis recorded for `docs/research/test-isolation-parallelism-research-*.md`.

### Completion Notes List

- **Shared helper home:** `parse_state_filter` + `state_matches` live in a new leaf module `crates/daemon/src/api/filter.rs` (the story's "or a new `api::filter` module if it reads cleaner" option), imported by `api::sessions`, `api::ws`, and `projection::snapshot` — no dependency cycle (the module only depends on `protocol::SessionCurrentState`).
- **WS `states` parse:** the field is `Vec<String>` on the wire; the daemon validates it **per element** via `parse_states_array` (review pass-1 finding #3) — each array entry is one state token, so a comma-bearing element like `"working,ended"` is rejected rather than silently split (one wire value, one grammar input, the same discipline `topic` follows). `parse_states_array` shares the single token table with the REST `?state=` parser (`crate::api::filter`); an empty vec is unfiltered.
- **Snapshot dedup model (review pass-2 findings F1–F3):** the per-connection snapshot dedup keys on the `(source, session_id)` rows already snapshot-delivered (`snapshotted_keys: HashSet<(String,String)>`), NOT on subscribed topics. A topic set cannot express a filtered subscribe's partial coverage; the per-key set makes every snapshot contract fall out of one model — identical re-subscribes are idempotent (F3), a wider re-subscribe re-sends only the keys a narrower one never covered (F1), and the `Unsubscribe` arm prunes keys no longer matched by any active subscription (via `Topic::covers_state_session`) so coverage lapses and a re-subscribe re-snapshots the drift (F2). This replaced pass-1's `fully_snapshotted: HashSet<Topic>` (unfiltered-only) approach, which fixed narrow→wide suppression but left F1/F2/F3 open. Cost: the per-topic pre-query SQL short-circuit is gone — an already-covered re-subscribe now reads the rows and dedups in Rust (immaterial at projection scale; the same cold-read tradeoff ADR 0008 accepted for Rust-side `?state=`).
- **`since`/`limit` SQL builder:** `db::queries::build_sessions_query(since, limit)` builds the SQL dynamically with `?` placeholders (option (a)); `build_sessions_query(0, None)` returns a string byte-identical to `SELECT_NON_SENTINEL_SESSIONS`, pinned by `unfiltered_query_matches_const` (AC #1). Bound via `rusqlite::params_from_iter` (a `Vec<i64>`, sidestepping the `Vec<Box<dyn ToSql>>` clippy `type_complexity` the 5.7 Dev Notes warned about).
- **axum query rejection is 400** (not 422) — confirmed by the `?since=notanint` and unknown-key tests; matches `EventsParams`. `?state=` invalid token is our own `400` with a `{"error": ...}` body naming the bad token + accepted set.
- **WS coverage substitution (Task 7 / Task 11):** no `wscat`/`websocat` (nor a node `ws` module) was installed, so the manual `wscat` snapshot-filter check was substituted by the e2e contract test `story_2_3_snapshot::snapshot_states_filter_excludes_ended_live_transition_still_arrives`, which drives a real daemon + real tungstenite client (snapshot excludes `Ended`; a live transition INTO `Ended` post-subscribe still arrives). The invalid-token close is covered by `story_2_1_ws::ws_invalid_states_token_closes_with_policy_violation` (real WS, 1008). The REST half of Task 11 was run live against the real graveyard DB (see Debug Log).
- **Smoke side-effect handled (environment note for the reviewer):** the maintainer's installed `~/.cargo/bin/bowerbird-daemon` was a pre-5.7 (v2-max) build; running the 5.8 (v3-aware) binary against the live `bower.db` ran migration v3 (additive `events.cwd` column, non-destructive — Story 5.7's migration, not new to 5.8), bumping the DB to `user_version=3`, after which the old binary could no longer open it. Recovery (maintainer-approved): `cargo install --path crates/daemon --force` installed the 5.8 build to `~/.cargo/bin`, then `bowerbird start` (one-time macOS Keychain Allow for the new binary). Daemon now healthy on the v3 DB. **No 5.8 migration was added** — `events`/`session_projections` are unchanged by this story.

- **Pass-3 review resolution (7 findings: 1 decision + 6 patches):** **F1 (decision):** a non-empty `states` on a non-`state.session.*` topic now closes 1008 (`Topic::is_state_session_family` gates the reject in the `Subscribe` arm) — chosen over documenting the silent no-op because the strict-inbound axiom is the inverse of Postel. **F2:** live State dispatch records its `(source, session_id)` key into `snapshotted_keys` in both the main `rx.recv()` arm and `drain_backlog_under_state`, so an identical re-subscribe no longer re-snapshots a row the live stream kept current. **F3:** the unsubscribe-coverage code was already correct (per-key prune via `Topic::covers_state_session`); reworded `protocol.md` to add the remaining-subscription caveat + new overlapping-subscription e2e. **F4:** `?limit=notanint`→400 and empty/trailing `?state=` token→400 (HTTP-layer). **F5:** new e2e for `states` on a specific `state.session.<id>` topic (matching + non-matching). **F6:** refreshed stale WS-parser wording (AC #10, Project Structure Notes, References, ADR 0008) to `parse_states_array` per-element (REST-side `parse_state_filter` references are correct, left as-is). **F7:** narrowed the `protocol.md` Topic Grammar rule to constrain the `topic` field only. New `Topic::is_state_session_family` also de-duplicates the snapshot-path gate in `snapshot_for_topic`.

### File List

**Source:**
- `crates/protocol/src/ws.rs` — `ClientMessage::Subscribe` gains `#[serde(default)] states: Vec<String>`.
- `crates/daemon/src/api/filter.rs` — NEW; `parse_state_filter` + `state_matches` shared helpers (+ unit tests).
- `crates/daemon/src/api/mod.rs` — `pub mod filter;`.
- `crates/daemon/src/api/sessions.rs` — `SessionsParams` (`Query` extractor), `list` validation→400 + `since`/`limit` to SQL + read-derived `?state=` filter in Rust, `bad_request` helper.
- `crates/daemon/src/db/queries.rs` — `build_sessions_query(since, limit)` dynamic builder (+ unit tests).
- `crates/daemon/src/projection/snapshot.rs` — `snapshot_for_topic` gains `state_filter: &[SessionCurrentState]` param + read-derived skip; pass-2 (F1–F3) replaced the topic-based dedup with a per-key `already_snapshotted: &HashSet<(String,String)>` skip and dropped the pre-query short-circuit (+ filter & per-key dedup tests); pass-3: the state-topic gate now calls `Topic::is_state_session_family()` (was an inline `matches!`).
- `crates/daemon/src/api/ws.rs` — `Subscribe` arm destructures `states`, parses via `parse_states_array` (bad token → `close_with_bad_message`), threads `&state_filter` into `snapshot_for_topic`; pass-2: per-connection `snapshotted_keys: HashSet<(String,String)>` records delivered keys, and the `Unsubscribe` arm prunes lapsed keys; **pass-3:** rejects a non-empty `states` on a non-state topic (F1), and records live-delivered State keys into `snapshotted_keys` in the `rx.recv()` arm + `drain_backlog_under_state` (F2; `drain_backlog_under_state`/`close_for_daemon_shutdown` gain the `snapshotted_keys` param); **pass-4:** both lag arms (`RecvError::Lagged` in the main loop + `TryRecvError::Lagged` in `drain_backlog_under_state`) now `snapshotted_keys.clear()` so a re-subscribe after a `dropped` frame re-snapshots (F1).
- `crates/daemon/src/broadcast/event.rs` — NEW `Topic::covers_state_session(session_id)` helper (backs the unsubscribe-time prune) (+ unit tests); **pass-3:** NEW `Topic::is_state_session_family()` helper (backs the F1 reject + the `snapshot_for_topic` gate).

**Tests:**
- `crates/protocol/tests/contract_protocol.rs` — `subscribe_without_states_deserializes_to_empty`, `subscribe_with_states_round_trips` (additive-compat, AC #13).
- `crates/daemon/tests/contract_daemon.rs` — `story_5_8_session_filter` REST module (10 tests incl. `sessions_unknown_query_key_400`), `story_2_1_ws::ws_invalid_states_token_closes_with_policy_violation`, `story_2_3_snapshot::{snapshot_states_filter_excludes_ended_live_transition_still_arrives, filtered_subscribe_does_not_suppress_later_wider_snapshot}`; pass-2 (F1–F3): `story_2_3_snapshot::{widening_filter_resends_only_uncovered_rows, identical_filtered_resubscribe_is_idempotent, unsubscribe_lapses_coverage_resubscribe_resnapshots}`; pass-3: `story_2_1_ws::{ws_states_on_event_topic_closes_with_policy_violation, ws_empty_states_on_event_topic_is_ordinary_subscribe}` (F1), `story_2_3_snapshot::{live_state_frame_recorded_in_coverage_no_duplicate_on_resubscribe (F2), overlapping_subscription_unsubscribe_does_not_lapse_covered_session (F3), states_filter_on_specific_session_topic (F5)}`, `story_5_8_session_filter::sessions_state_filter_empty_and_trailing_token_400` + `sessions_limit_caps_rows` `limit=notanint` (F4); pass-4 (F1): `story_2_4_dropped::lag_invalidates_snapshot_coverage_resubscribe_resnapshots` (lag clears snapshot coverage; re-subscribe after `dropped` re-snapshots).

**Docs:**
- `docs/protocol.md` — `GET /sessions` `?state=`/`?since=`/`?limit=` + WS `Subscribe.states` snapshot filter.
- `docs/protocol-changelog.md` — one `type: schema` entry under v1.0 → v1.1 (`Resolves: 5.8`).
- `docs/presenter-authoring.md` — triage note (filter server-side, not client-side).
- `docs/bmad/project-context.md` — §Wire format (additive inbound `Subscribe.states`) + §HTTP surface (`/sessions` filters), per ADR 0008 `Affects context.md sections`.

**Planning artifacts:**
- `docs/bmad/planning-artifacts/epics.md` — §Story 5.8 stub replaced with real ACs (no renumber).
- `docs/bmad/implementation-artifacts/sprint-status.yaml` — status transitions + breadcrumbs.
- `docs/bmad/implementation-artifacts/deferred-work.md` — "No pagination on `GET /sessions`" marked partially-addressed-by-5.8 (true cursor pagination still deferred).
- bean `gt-3cnt` — filter half noted resolved; retention sweep stays open.

### Change Log

- 2026-06-02 — Story 5.8 implemented (ADR 0008): `GET /sessions` `?state=`/`?since=`/`?limit=` query filters + optional `ClientMessage::Subscribe.states` snapshot-scoping filter, both default-unfiltered, filtering on the read-time `current_state`. No schema migration. Shared `api::filter` helper. 15 ACs satisfied; 537 workspace tests + fmt + clippy + changelog gate green; REST smoke validated live against the real graveyard DB. Status ready-for-dev → in-progress → review.
- 2026-06-02 — Code-review findings resolved (8 items: 1 decision + 7 patches). #1 (since/limit semantics): kept recency-filter + cap, corrected `protocol.md` to drop the "paginate via `?since=`" framing (added the no-backward-cursor / boundary-tie / read-time-staleness gotchas) and downgraded the `deferred-work.md` entry from resolved to partially-addressed (true cursor pagination still deferred). #2: snapshot dedup now keys on a `fully_snapshotted` set (unfiltered snapshots only) so a filtered subscribe never suppresses a later wider one — new test `filtered_subscribe_does_not_suppress_later_wider_snapshot`. #3: WS `states` validated per array element via `parse_states_array` (comma-bearing element rejected), not CSV-joined. #4: `ClientMessage` derives `Serialize`/`PartialEq`; `subscribe_with_states_round_trips` now truly round-trips; unfiltered serialize matches the v1.0 wire shape via `skip_serializing_if`. #5: new `sessions_unknown_query_key_400` test. #6: removed the real `sleep`s from the WS states-filter test (exclusion proven by frame ordering, not wall-clock). #7: corrected the Task 11 WS-smoke record (automated e2e, not manual `wscat`) and this AC count (15, not 14). #8: the 5.1/5.6/5.7 + architecture churn was split into its own commit. Status review → in-progress (code-review) → review.
- 2026-06-02 — Code-review pass-2 findings resolved (5 items: 1 decision + 4 patches). **F1 (decision — snapshot dedup contract):** replaced pass-1's `fully_snapshotted: HashSet<Topic>` (unfiltered-only) with per-`(source,session_id)`-key coverage (`snapshotted_keys`), grounded in the project's natural-key invariant + the `protocol.md` no-double-delivery promise + the snapshot's catch-up purpose. One model resolves F1/F2/F3: a widening re-subscribe re-sends only newly-uncovered keys, identical re-subscribes are idempotent, and (F2) the `Unsubscribe` arm prunes keys no longer matched by any active subscription (new `Topic::covers_state_session`) so a re-subscribe re-snapshots the drift. Dropped the per-topic pre-query SQL short-circuit (cold-read tradeoff). `protocol.md` §subscribe documents the contract. New e2e tests: `widening_filter_resends_only_uncovered_rows`, `identical_filtered_resubscribe_is_idempotent`, `unsubscribe_lapses_coverage_resubscribe_resnapshots`; snapshot unit tests reworked to the per-key model; `covers_state_session` unit tests. **F4:** aligned ADR 0008 (§31, §50), `epics.md` §5.8, and the story AC #7/#14/Task 9/Dev Notes/References/File List to the final "recency filter + row cap, NOT true cursor pagination (still deferred)" wording (matching `protocol.md:78` + `deferred-work.md:53`). **F5:** refreshed stale story records (Completion Notes WS-parse note → `parse_states_array` per-element; Task 5 → `snapshotted_keys`/per-key dedup + unsubscribe prune; Task 11 title → REST-manual / WS-automated-e2e). Status review → in-progress (code-review pass-2) → review.
- 2026-06-03 — Code-review pass-3 findings resolved (7 items: 1 decision + 6 patches). **F1 (decision — `states` on non-state topics):** REJECT — a non-empty `states` on a non-`state.session.*` topic now closes 1008 (new `Topic::is_state_session_family`); an empty `states` stays valid on any topic. Chosen over documenting the silent no-op: the strict-inbound axiom is the inverse of Postel, so a silently-discarded valid filter is the silent lie the policy forbids, and the reject removes the indefensible valid-vs-invalid-token asymmetry. New e2e `ws_states_on_event_topic_closes_with_policy_violation` + `ws_empty_states_on_event_topic_is_ordinary_subscribe`; ADR 0008 §Decision + `protocol.md` §subscribe updated. **F2:** live State dispatch now records its `(source, session_id)` key into `snapshotted_keys` in both the `rx.recv()` arm and `drain_backlog_under_state`, so an identical re-subscribe no longer re-snapshots a row the live stream kept current — new e2e `live_state_frame_recorded_in_coverage_no_duplicate_on_resubscribe`. **F3:** the unsubscribe-prune code was already correct; reworded `protocol.md` so coverage lapses only when no remaining active state subscription covers the session — new overlapping-subscription e2e `overlapping_subscription_unsubscribe_does_not_lapse_covered_session`. **F4:** `?limit=notanint`→400 and empty/trailing `?state=` token→400 at the HTTP layer (`sessions_state_filter_empty_and_trailing_token_400`). **F5:** new e2e `states_filter_on_specific_session_topic` (matching + non-matching filter on `state.session.<id>`). **F6:** refreshed stale WS-parser wording (AC #10, Project Structure Notes, References, ADR 0008 §Decision/§Alternatives) to per-element `parse_states_array` sharing the `parse_state_token` table with REST. **F7:** narrowed the `protocol.md` Topic Grammar rule to constrain the `topic` field only (the sibling `states` field is an array). Status review → in-progress (code-review pass-3) → review.
- 2026-06-03 — Code-review pass-4 findings resolved (5 items: all patches). **F1 (code+test):** both broadcast-lag paths — the main `rx.recv()` `RecvError::Lagged` arm and `drain_backlog_under_state`'s `TryRecvError::Lagged` arm — now `snapshotted_keys.clear()` once lag is observed with a non-empty subscription set. `Lagged(n)` reports only a count, never the evicted envelopes, so the gap may have carried a `state` frame for a covered session; conservatively invalidating ALL coverage lets a re-subscribe after a `dropped` frame re-snapshot and recover (a state-only subscriber can't replay missed state via `/sessions/{id}/events?since=`). New e2e `story_2_4_dropped::lag_invalidates_snapshot_coverage_resubscribe_resnapshots` (verified red without the fix). **F2 (docs):** `protocol.md` §subscribe now describes coverage as rows held current by snapshot OR live `state` frame, and documents lag-invalidation. **F3 (docs):** `docs/decisions/0008-server-side-session-filter.md:49` + the story's "Files this story touches" bookkeeping de-paginated — exact-N pages still require deferred cursor pagination. **F4 (bookkeeping):** `sprint-status.yaml` collapsed to exactly one active `last_updated:` key (history stays commented), `development_status` in lockstep at `review`. **F5 (docs):** `protocol-changelog.md` 5.8 entry + `protocol.md` `/sessions` now name the strict-reject edges (unknown query key → 400, per-element WS validation, non-state-topic `states` → 1008). Status review → in-progress (code-review pass-4) → review.
