# protocol-changelog

This changelog is CI-enforced (Story 4.4, FR39). Any PR that modifies
`crates/protocol/src/*.rs` MUST add at least one entry under the active
version section below with a `type:` header — one of `type: schema`,
`type: behavioral`, or `type: security`. The gate is implemented as a
compiled workspace-root test at `tests/protocol_changelog_gate.rs` that
runs under `cargo test --workspace -- --test-threads=1`; see the test's
source comment for the exact diff-parsing rules.

The discipline is documented in
[docs/bmad/planning-artifacts/epics.md FR39](bmad/planning-artifacts/epics.md)
and Story 4.4
([docs/bmad/implementation-artifacts/4-4-protocol-compatibility-guarantee-and-contract-test-suite.md](bmad/implementation-artifacts/4-4-protocol-compatibility-guarantee-and-contract-test-suite.md)).
"Compiled tests beat greps" per Epic 3 retrospective Team Agreement A7 —
the gate fires on the same workspace-test lane CI already runs, so a PR
that skips it is the same PR that skips the test suite.

## v1.0 → v1.1

- **type: schema** — Added `SessionCurrentState` enum (`Idle`, `Working`, `WaitingInput`) and `SessionState` struct (`current_state`, `last_event_kind`, `last_event_at_ms`) for per-session current-state projection. Story 1.6, FR25.
- **type: schema** — Added `protocol::rest::SessionListItem` (`source`, `session_id`, `current_state`, `last_event_kind`, `last_event_at_ms`, `updated_at`) — list-shaped row for `GET /sessions` responses. Story 1.7.
- **type: schema** — Added `protocol::rest::SessionDetail` (`source`, `session_id`, `state`, `updated_at`) — body shape for `GET /sessions/{id}` responses. Story 1.7.
- **type: schema** — Added `protocol::rest::DaemonStatus` (`daemon_version`, `protocol_version`, `started_at_ms`, `uptime_ms`, `last_event_at_ms`, `last_event_id`) — body shape for `GET /status` responses. `connected_ws_clients` is deferred to Story 3.2 alongside the `bowerbird status` CLI (its first V1 consumer). Epic 2 shipped the semaphore infrastructure (`AppState::ws_semaphore`); only the `DaemonStatus` surfacing was deferred. Story 1.7.
- **type: behavioral** — `GET /sessions`, `GET /sessions/{id}`, `GET /sessions/{id}/events?since=<cursor>`, `GET /sessions/{id}/stats`, `GET /status` are now live. All five require a bearer token via `Authorization: Bearer <token>`. `GET /healthz` and `GET /readyz` remain unauthenticated. Story 1.7.
- **type: behavioral** — `GET /readyz` now also probes the database (`SELECT 1 FROM events WHERE 1=0` against the reader pool). A 200 implies both `migrations_complete` AND `db_probe_ok`; previously only the migrations branch was checked. Story 1.7, AC #2. Resolves the deferred-work item from the Story 1.2 review.
- **type: behavioral** — Bearer-token resolution chain for v1.x is `BOWERBIRD_TOKEN env → generated UUID4 (WARN-logged)`. The full `keychain → env → file` chain documented in `architecture.md:442` is reserved for Story 3.3. The validation layer (`api::auth::require_bearer`, `BearerToken::verify`) does not change between v1.7 and 3.3 — only the issuance source. (`Resolves-In: 3.3`) (Resolved in the Story 3.3 entry below.)
- **type: behavioral** — Ingest socket: `hook_kind` is now required. Story 1.8. The daemon previously accepted ingest lines without a `hook_kind` field and silently treated them as `PreToolUse`. As of this release, a missing or non-string `hook_kind` is rejected with `400 missing hook_kind\n` and any `hook_kind` not in the adapter's known set is rejected with `400 unknown hook_kind: <value>\n` (the `<value>` is the offending string sanitized via `sanitize_for_wire`). The wire JSON schema is unchanged; only the daemon's tolerance for malformed input tightened. Tools that only consume the daemon's outbound surface (REST, WS) are unaffected. The shim already injects `hook_kind` on every payload (Story 1.5) — no shim-side changes required. Resolves the deferred-work item from the Story 1.4 review.
- **type: schema** — **WebSocket surface live at `GET /ws`** (Story 2.1). Authenticated via bearer token in `Authorization: Bearer <token>` header (preferred) or `?token=<token>` query parameter (fallback for clients that cannot set headers, e.g. browser `new WebSocket()`). When the `Authorization` header is present (any value, including malformed), it wins — the query token is NOT consulted as a fallback. On upgrade, the daemon sends one `hello` `ServerMessage` containing `protocol_version`, `daemon_version`, `oldest_available_event_id`, `daemon_started_at`, and `history_begins_cleanly`. Inbound `ClientMessage` uses strict `deny_unknown_fields`: `{"op":"subscribe","topic":...}` and `{"op":"unsubscribe","topic":...}` are the only valid shapes (single topic per message). Supported topics are `events.*`, `events.<source>.*`, `events.<source>.<session_id>`, `state.session.*`, `state.session.<id>`, `state.session.<id>.current_state`. Unknown topics, empty topics, unknown ops, extra fields, binary frames, and non-JSON payloads close the connection with WS close code 1008 (Policy Violation) and a `bad message: ...` reason (sanitized, capped at 123 bytes per RFC 6455 §5.5.1). Outbound `ServerMessage` adds a `State(StateFrame { source, session_id, state })` variant. To make new variants additive across v1.x in practice — not just on paper — `ServerMessage` now carries a `#[serde(other)] Unknown` catch-all on deserialize; older clients (or third-party bindings) using this crate at an earlier version decode future variants as `Unknown` instead of failing on the tag. The asymmetric `deny_unknown_fields` policy alone only covers struct fields, not enum variants; this catch-all closes the gap. Concurrent connection cap is 256 (configurable via `Config::ws_max_connections`); over-cap upgrades return HTTP 503 without upgrading. Idle ping interval is 30s, pong timeout is 10s; on pong timeout the connection closes (deadline-granularity, not next-tick-granularity). Story 2.1 introduced the WS surface and topic grammar; the broadcast-hub publish path was wired into `projection::session::write` by Story 2.2 (see the behavioral entry below).
- **type: behavioral** — **HTTP surface now emits `x-request-id` and respects a 30s per-request timeout and a 1 MiB request-body limit** (Story 2.1). Every HTTP request (including WS upgrade requests up to the point of upgrade) receives an `x-request-id` UUID4 header on the response, propagated for cross-cut tracing. HTTP request handlers (NOT WebSocket connections once upgraded) are bounded by a 30-second wall-clock timeout; requests exceeding it receive HTTP `408 Request Timeout`. Request bodies larger than 1 MiB receive `413 Payload Too Large` from the underlying `RequestBodyLimit` layer. WebSocket connections, once the upgrade is complete, are exempt from both. Existing clients that completed all v1.0 requests in under 30s and stayed under 1 MiB are unaffected. Resolves the deferred-work entry from the Story 1.7 review.
- **type: behavioral** — **`event` and `state` `ServerMessage` frames are now broadcast to WS subscribers** (Story 2.2). Every successful `projection::session::write` (the ingest path's single canonical publisher for non-sentinel events) publishes one `BroadcastEnvelope::Event` followed by one `BroadcastEnvelope::State` after the SQLite transaction commits. WS subscribers to `events.*`, `events.<source>.*`, `events.<source>.<session_id>`, `state.session.*`, `state.session.<id>`, and `state.session.<id>.current_state` (all topics shipped in 2.1) now receive live frames; previously these topics accepted subscriptions but never delivered. Sentinel events with `source = "__daemon__"` (`RecordingStarted`, `RecordingEnded`) are NOT published — daemon lifecycle stays off the user-facing surface. Publishing is gated on `tx.commit()` success: a write that fails to commit produces zero broadcast envelopes. Multiple subscribers are fully independent — closing one WS connection does not affect frame delivery to other subscribers, and the closed connection's semaphore permit is released. End-to-end latency targets hook→presenter p99 ≤100ms (NFR2); not a hard contract-test gate in this release, will be benchmarked separately.
- **type: behavioral** — **WebSocket `state.*` subscriptions now receive a snapshot of matching session projections on subscribe** (Story 2.3). A `Subscribe` for `state.session.*`, `state.session.<id>`, or `state.session.<id>.current_state` causes the daemon to read `session_projections` (sentinel-excluded, ordered `updated_at DESC, source ASC, session_id ASC`) and emit one `ServerMessage::State` frame per matching session BEFORE any subsequent live frame. Frames apply the read-time stale-Working → Idle fallback (`current_state_for_read`) just like `GET /sessions` and `GET /sessions/{id}`. Snapshots are deduped against the pre-existing subscription set on the same connection: subscribing to `state.session.*` then `state.session.<id>` emits zero additional snapshot frames for that id (the wildcard already covered it). Subscriptions to `events.*`-family topics emit ZERO snapshot frames — event history is fetched via REST `GET /sessions/:id/events?since=0` (Story 1.7), not via WS replay. New sessions that appear after subscribe-time still arrive as live `State` frames via Story 2.2's publish path (no reconnect, no re-subscribe, no daemon-side bookkeeping). Snapshot frames are sent directly on the requesting connection's socket, never through the broadcast hub, so other subscribers are unaffected.
- **type: schema** — `SyncFrame::new(oldest_available_event_id, latest_event_id) -> Result<Self, Error>` constructor added (Story 2.3, fold-in from the Epic 1 retro). Rejects `oldest > latest` at construction time with `Error::InvalidSyncFrameOrdering { oldest, latest }`. The serde shape of `SyncFrame` is unchanged — wire payloads (including ones from a hypothetical buggy peer with inverted IDs) continue to round-trip through `Deserialize` without validation. The asymmetric inbound/outbound policy is preserved: tighten outbound construction, leave inbound parsing permissive. No Story 2.3 producer activates the constructor yet; Story 2.4's lagged-consumer recovery is the likely first call site.
- **type: behavioral** — **Lagged WS consumers now receive a `dropped` `ServerMessage` frame** instead of silent skip (Story 2.4). When a per-connection broadcast receiver falls more than `ws_broadcast_capacity` positions behind the publisher, the daemon emits one `DroppedFrame { count, first_dropped_event_id, last_dropped_event_id }` to that socket; `count` is in **envelopes** (not bytes); `first_dropped_event_id` and `last_dropped_event_id` are best-estimate upper-bound values (the broadcast channel does not expose the post-lag cursor synchronously). Presenters MUST recover via REST `GET /sessions/{id}/events?since=<last_delivered_event_id>` where `last_delivered_event_id` is the cursor the presenter authoritatively tracked from prior `event` frames it received — NOT the ids inside the `dropped` frame. Gap-recoverability is the presenter's derivation: `oldest_available_event_id <= last_delivered_event_id + 1`. Lag bursts are coalesced over a sliding `coalesce_window` (default 1s, configurable via `Config::ws_broadcast_coalesce_window`); sustained lag emits a bounded number of `dropped` frames per window, not one per `Lagged` recv. Both the main `rx.recv()` arm and the Subscribe/Unsubscribe drain path route through the same coalescing helper. The socket stays open after `dropped` emission per AC #1, and `last_delivered_event_id` continues to advance on subsequent Event frames.
- **type: schema** — `DroppedFrame::new(count, first_dropped_event_id, last_dropped_event_id) -> Result<Self, Error>` constructor added (Story 2.4, fold-in from the Epic 1 retro / deferred-work.md:9). Rejects `count == 0` and `first > last` at construction time with `Error::InvalidDroppedFrame { count, first, last }`. `DroppedFrame` is now `#[non_exhaustive]` to block struct-literal construction from outside the `protocol` crate; daemon producers must route through `new`. The serde shape is unchanged — wire payloads continue to round-trip through `Deserialize` without validation. Asymmetric inbound/outbound policy preserved, mirroring `SyncFrame::new` (Story 2.3).
- **type: behavioral** — Graceful daemon shutdown now sends each connected WebSocket client a protocol `close` `ServerMessage` with `reason = "daemon shutdown"` before the WebSocket control close (Story 2.5). SIGTERM and SIGINT share the same path: stop accepting HTTP/WS upgrades and ingest connections, drain accepted ingest writes, drain each subscribed connection's broadcast backlog, emit the protocol close, wait up to 5 seconds for WS tasks, then commit the daemon lifecycle sentinel and checkpoint the WAL. `RecordingEnded` remains a database lifecycle sentinel only and is not broadcast to user-facing WS clients. No schema change: `ServerMessage::Close(CloseFrame)` already existed.
- **type: behavioral** — Daemon lifecycle CLI surface (Story 3.2). Two additive protocol-crate changes consumed by the new `bowerbird start` / `bowerbird status` subcommands, both landed in the same release. (1) `protocol::ServerInfo { bind_addr: String }` added to `crates/protocol/src/rest.rs` — describes the `~/.bowerbird/server.json` file the daemon now publishes once `listener.local_addr()` resolves the kernel-assigned ephemeral port; the CLI reads this to find the daemon's HTTP surface for `/healthz` and `/status` probes. The file is written atomically (tmp + fsync + rename) with mode `0600` and best-effort deleted on clean shutdown; after an unclean exit the file is left on disk with a possibly-stale `bind_addr`, so the CLI treats it as a hint rather than a liveness proof (the singleton PID + ingest-socket connect probe are the authoritative checks). Story 3.3 will extend `ServerInfo` with a `token` field — the asymmetric `deny_unknown_fields` policy applies per-direction (daemon-controlled outbound emission to a sibling CLI process), so the absence of `deny_unknown_fields` here is deliberate and documented in the type's doc comment. (2) `protocol::DaemonStatus.connected_ws_clients: u32` added as an additive field after `last_event_id`; the value is computed as `ws_max_connections - ws_semaphore.available_permits()` at request time and reflects WebSocket connections that have completed the upgrade past `try_acquire_owned` and not yet released their `OwnedSemaphorePermit`. Because the semaphore read is a momentary snapshot — not snapshot-consistent across calls — the field is intentionally documented as a count-at-a-point-in-time, not a high-precision metric. v1.0 presenters continue to deserialize `DaemonStatus` responses without modification (the outbound types are permissive on extra fields per the asymmetric serde policy; additive fields are silently ignored by older bindings). Closes Epic 2 retrospective AI-1 / Story 1.7 deferred-work entry for `/status.connected_ws_clients`.
- **type: behavioral** — Bearer-token resolution chain extended (Story 3.3). The daemon now resolves its bearer token in this order: (1) `BOWERBIRD_TOKEN` env var, (2) system keychain via the `keyring` v3 crate (`service = "bowerbird-daemon"`, `user = "bearer-token"`; macOS Keychain on Darwin, Secret Service via D-Bus on Linux), generating and storing a UUID4 on first run if the entry is empty, (3) `~/.bowerbird/config.toml` `token` field (mode 0600 expected; warning on wider mode but value still used). When all three fail, the daemon exits non-zero with a human-readable error to stderr enumerating each tried path (NFR13). The validation layer (`api::auth::require_bearer`, `BearerToken::verify`) and the on-wire `Authorization: Bearer <token>` header format are unchanged — v1.0 presenters continue to authenticate identically. The previous behavioral entry for Story 1.7's `env → generated UUID4` chain is now superseded by this full chain; tools that relied on the auto-generated fallback continue to work (the keychain write-and-persist fills the same role with persistence). New CLI surface: `bowerbird auth token` prints the resolved bearer token to stdout (followed by `\n`, no banner, no prefix; pipe-safe into `Authorization: Bearer $(bowerbird auth token)`). The `bowerbird status` CLI now auto-resolves the token via the same chain — the user no longer needs to set `$BOWERBIRD_TOKEN` to read `/status` details, redeeming the Story 3.2 `"or wait for Story 3.3"` placeholders. A test-only `BOWERBIRD_KEYRING_BACKEND={default|disable|mock}` env override exists for hermetic test runs; the `mock` value is feature-gated behind the daemon's `mock-keyring` cargo feature (defaulted ON for `cargo test --workspace`; release distributions can build with `--no-default-features` so a malicious env-var injection cannot neutralize keychain protection). The CLI mirrors the daemon's chain (env → keychain → config.toml) and does NOT depend on `crates/daemon` (CLI stays tokio-free; the keychain service+user constants are duplicated with cross-pointer doc comments). (`Resolves: 3.3`)
- **type: behavioral** — `protocol::SHIM_BINARY_NAME` value changed from `"bowerbird"` to `"bowerbird-shim"` (Story 3.1, Task 5, Approach B). The constant's identifier stays the same; only the string value it holds changes. This reconciles the constant with the actual hot-path binary name declared in `crates/shim/Cargo.toml` (`[[bin]] name = "bowerbird-shim"`) and frees the unadorned `bowerbird` name for the user-facing CLI binary (top-level `Cargo.toml` `[[bin]] name = "bowerbird"`) introduced by `bowerbird install` / `bowerbird uninstall` in the same story. `bowerbird install` writes the hook command `bowerbird-shim --hook-kind <kind>` into `~/.claude/settings.json` under each known hook kind. The shim source code, its `release-shim` profile, and the Story 1.5 p99 ≤5ms hot-path budget are unchanged — only the protocol constant value rotated to match the binary's actual name. Presenters that read `SHIM_BINARY_NAME` from the protocol crate at runtime see the new value automatically; presenters that hard-coded `"bowerbird"` as a path component need to update to `"bowerbird-shim"`.
- **type: behavioral** — Prebuilt binary distribution shipped (Story 3.4). GitHub Releases now attach three prebuilt tarballs per tag: `bowerbird-vX.Y.Z-aarch64-apple-darwin.tar.gz` (macOS arm64), `bowerbird-vX.Y.Z-x86_64-apple-darwin.tar.gz` (macOS x86_64; cross-compiled from arm64 runner via `rustup target add x86_64-apple-darwin`), and `bowerbird-vX.Y.Z-x86_64-unknown-linux-gnu.tar.gz` (Linux x86_64, glibc 2.35+ — pinned to `ubuntu-22.04` runner so the Linux baseline covers Ubuntu 22.04/24.04, Debian 12+, RHEL 9.0+). Each tarball contains the three binaries (`bowerbird`, `bowerbird-shim` built under the `release-shim` profile for the perf budget, `bowerbird-daemon`), the bundled `tool-reactions.toml` data file under `adapters/claude/`, MIT+Apache-2.0 license files (workspace now dual-licensed per Rust ecosystem convention), the project `README.md`, and `INSTALL.md` with the post-extract walkthrough. SHA-256 checksums are attached alongside each tarball. The release pipeline at `.github/workflows/release.yml` triggers on `v*.*.*` tag pushes or via `workflow_dispatch`; the release-notes template names the artifacts and restates the musl-deferral statement verbatim per AC #2. musl Linux is deferred post-V1 (NFR9); the alternative install path is `cargo install --git https://github.com/<owner>/bowerbird --tag vX.Y.Z` from a stable Rust toolchain (NFR10; no nightly required; `Cargo.lock` committed; `--locked` enforced in CI). Crates.io publishing is also deferred post-V1 — V1 distribution is GitHub-Releases-only (prebuilt) plus git-clone (source). The `bowerbird install` command's hook entry uses the PATH-relative binary name `bowerbird-shim --hook-kind <KIND>` (Story 3.1 AC #1 / Story 3.4 AC #4; pinned by `installed_command_uses_path_relative_binary_name_no_slash_in_first_token` regression test in `crates/adapter-claude/tests/contract_install.rs`) so users who re-download to a different `$PATH` location are picked up automatically without re-running `bowerbird install`. The CI workflow (`.github/workflows/ci.yml`) now serializes the workspace test suite with `cargo test --workspace -- --test-threads=1` per Epic 2 retro AI-3 (Discovery #3) — the daemon contract suite and CLI E2E suites share process-wide state (subprocesses, signal handlers, `BOWERBIRD_DATA_DIR`, `BOWERBIRD_KEYRING_BACKEND`) and parallel execution caused hangs in Stories 1.6, 2.5, 3.1, 3.2, 3.3. The architecture.md WebSocket subsystem section now documents the six runtime config knobs (`ws_max_connections`, `ws_ping_interval`, `ws_pong_timeout`, `ws_broadcast_capacity`, `shutdown_drain_timeout`, `ws_broadcast_coalesce_window`) with defaults sourced verbatim from `crates/daemon/src/config.rs::Config::with_bowerbird_dir` (Epic 2 retro AI-2 / Discovery #2). No wire-protocol surface changes. (`Resolves: 3.4`)
- **type: schema** — New authenticated REST endpoint `POST /replay` (Story 4.1). The endpoint accepts a JSONL request body of `protocol::Event` records (post-store form, same shape `GET /sessions/{id}/events` returns); for each line, the daemon constructs a `protocol::EventEnvelope` (dropping the line's `event_id` and `created_at`, which the daemon reassigns at projection-write time via AUTOINCREMENT + `current_unix_millis()`), and pushes the envelope onto the existing `ingest_tx` channel that the Unix-socket ingest path already uses. Replayed events therefore flow through the same `ingest::writer::run` → `projection::session::write` path as live shim ingest — persisted to `events`, projected into `session_projections`, broadcast via `BroadcastHub::publish`. WS subscribers receive `EventFrame` + `StateFrame` pairs in JSONL line order; multi-session JSONL files demonstrate multi-session fan-out by virtue of the existing per-session UPSERT semantics. The response body is `{"replayed_count": N, "parse_errors": [{"line": M, "error": "..."}]}` where parse errors are per-line and the handler continues on failure — replay is a development tool, not a transactional ingest path. Sentinel kinds (`RecordingStarted` / `RecordingEnded`) are rejected at the replay boundary with a parse-error entry; they remain reserved for daemon-lifecycle emission. No rate-limiting on the endpoint (replay is for development; the existing `RequestBodyLimitLayer::new(1 MiB)` cap is the only structural limit, and a future story can add streaming if larger replays become routine). Original inter-event timing is NOT preserved — events are forwarded as fast as the bounded `ingest_tx` channel accepts them, and `created_at` reflects replay wall-clock rather than source wall-clock. The complementary CLI command `bowerbird export <session-id>` reads `/sessions/{id}/events?since=<cursor>` in a cursor-paginated loop and writes JSONL to stdout (or `-o <path>`) in the same wire shape, so `bowerbird export <id> | bowerbird replay /dev/stdin` round-trips a session through the pub/sub path. The bundled demo fixture at `fixtures/replay-demo.jsonl` is embedded into the `bowerbird` CLI binary via `include_bytes!` and used by `bowerbird replay` (no-arg) so new users can exercise the pub/sub path without a live Claude Code session. v1.0 presenters are unaffected — they do not call `/replay` and are not subscribed to a replay-specific broadcast topic. (`Resolves: 4.1`)
- **type: schema** — Wire-surface enum catch-all sweep (Story 4.4, Epic 2 retro AI-4 fold-in). Added `#[serde(other)] Unknown` to `EventKind` (`crates/protocol/src/event.rs`) and `SessionCurrentState` (`crates/protocol/src/state.rs`). Changed `Reaction::deserialize` (`crates/protocol/src/reaction.rs`) to map unknown reaction strings to `Reaction::Unknown` rather than returning a deserialize error — this is the load-bearing **behavioral** fix the sweep surfaced (a future v1.x reaction string like `"Block"` would have failed to decode on v1.0 clients, breaking the additive-compat claim). `ServerMessage::Unknown` (Story 2.1) is unchanged; `ClientMessage` stays strict by design (inbound `deny_unknown_fields` is correct policy — unknown ops are protocol violations the daemon rejects with WS close code 1008 + `bad message: unknown op`). `Error` (`crates/protocol/src/error.rs`) is never directly wire-serialized — HTTP error responses use `{"error":"..."}` JSON bodies, ingest socket uses `400 <reason>\n` plain text, WS close uses RFC 6455 §5.5.1 reason strings — so no catch-all variant needed; module doc comment added clarifying the rationale. The asymmetric `deny_unknown_fields` policy alone only covers struct fields, not enum variants; this sweep is what makes "additive within v1.x" real for future-shipped variants of these four outbound types. v1.0 presenters continue to deserialize all five enums without modification (additive-only). The daemon never CONSTRUCTS `Unknown` — adapter `normalize` rejects unknown hook strings at the boundary, projection layers preserve prior state on Unknown via defense-in-depth match arms, and `event_kind_as_str` debug-asserts against Unknown at the SQLite persistence boundary. `/replay` rejects Unknown at parse time with a clear "this build is older than the source daemon" message so an operator updating bowerbird from a future capture file does not silently persist the lossy `"Unknown"` token. Closes Epic 2 retrospective AI-4. (`Resolves: 4.4`)
- **type: behavioral** — Ingest-socket framing rationale (Story 4.4, Epic 1 retro Agreement A3 carryover / Epic 2 retro AI-6). The NDJ framing on `~/.bowerbird/ingest.sock` (one `{object}\n` in, one status line out per [ADR-0002](decisions/0002-ingest-wire-framing-and-hook-kind.md)) is a deliberate choice for **shim-dependency minimalism** — the shim is `std`-only with no async runtime, and any framing more complex than "write a line, exit" would require pulling in a parser or state machine that violates the hot-path budget. It is NOT a latency optimization; the latency budget (p95 ≤5ms shim per Story 1.5 + Story 4.4 AC #4; p99 ≤100ms hook→presenter per NFR2 + Story 4.4 AC #7) is met DESPITE the framing being simple, not BECAUSE of it. Future presenter authors building custom shims should understand the constraint hierarchy: minimal-dependency shim first, then latency budget within that constraint. The Story 4.3 `docs/protocol.md` §Ingest socket contract section already narrates this; this changelog entry preserves the rationale in the change-history record so it survives a future `docs/protocol.md` rewrite. No wire-shape change. Closes Epic 1 retrospective Agreement A3 carryover and Epic 2 retrospective AI-6. (`Resolves: 4.4`)
- **type: behavioral** — State-broadcast emission tightened to `current_state`-transitions-only (Story 5.2). Previously every successful `projection::session::write` emitted one `BroadcastEnvelope::State` after the SQLite commit (Story 2.2). As of this release, the daemon compares the prior and new `SessionState` records read in the same transaction and emits a `State` envelope only when `current_state` differs; writes that update `last_event_kind` or `last_event_at_ms` without a state transition produce zero `State` envelopes (the `Event` envelope is unchanged — every event still publishes one). The first event for a previously-unknown session counts as a transition (`None → Some(<state>)`), so a new session always produces an initial `State` envelope. Snapshot-on-subscribe semantics (Story 2.3) are unchanged: `state.*` subscribers still receive an initial snapshot frame per matching session before live frames begin. Presenters that compute time-since-last-event from `state.session.*` must move that computation to the `events.*` stream (the `last_event_at_ms` field on the state row remains accurate for REST responses and snapshots, but is no longer pushed on every event). Backward-compatible: v1.0 presenters receive strictly fewer state envelopes; no schema change; no new error condition. The new behavior is pinned by the `state_broadcast_only_on_transition` contract test in `crates/daemon/tests/contract_daemon.rs`. Resolves the Story 5.2 `sprint-change-proposal-2026-05-27.md` dogfooding finding about ribbon-card flap. (Story renumbered from 5.7 → 5.2 by `sprint-change-proposal-2026-05-27-epic-5-resequencing.md`.) (`Resolves: 5.2`)
- **type: schema** — `UserPromptSubmit` `EventKind` variant added and subscribed (Story 5.2). New variant in `crates/protocol/src/event.rs` emitted when the user submits a prompt to Claude Code; the `adapter-claude` crate now subscribes the `UserPromptSubmit` hook (Claude Code hook kind of the same name) and normalizes it in `crates/adapter-claude/src/normalize.rs`. `bowerbird install` writes five hook entries to `~/.claude/settings.json` (was four); `bowerbird uninstall` removes all five; the install command detects pre-5.2 four-hook installs and surfaces a re-run hint. State-machine semantics in `crates/daemon/src/projection/state.rs`: the `UserPromptSubmit` arm of `transition()` returns `SessionCurrentState::Working`. The companion behavioral change (PostToolUse no longer flips to `Idle`; it now preserves `prev.current_state`) lands in the same release — see the state-broadcast tightening entry above for the user-visible motivation. The 5-minute `STALE_WORKING_MS` read-time fallback (Story 1.6) is unchanged and now backstops both a dropped `Stop` hook AND the legacy missing-`PostToolUse` case. The `hook_kind` ingest contract (Story 1.8) extends from four accepted values to five (`docs/protocol.md:334`). v1.0 presenters compiled against the pre-5.2 protocol enum decode `kind: "UserPromptSubmit"` as `EventKind::Unknown` via the Story 4.4 `#[serde(other)]` catch-all — the event still parses and the connection stays open. Additive within v1.x. Resolves the Story 5.2 `sprint-change-proposal-2026-05-27.md` dogfooding finding about user-prompt-to-first-tool gap reading `Idle`. (Story renumbered from 5.7 → 5.2 by `sprint-change-proposal-2026-05-27-epic-5-resequencing.md`.) (`Resolves: 5.2`)
- **type: schema** — `SessionState.last_pid: Option<u32>` field added (Story 5.3). New field on `crates/protocol/src/state.rs::SessionState` carries the most recent envelope's `pid` per the "overwrite-on-Some, carry-forward-on-None" rule (`pid.or(prev.and_then(|s| s.last_pid))`). `protocol::rest::SessionListItem` gains the same field for parity with `SessionDetail.state.last_pid`. The wire form is a JSON `number` (u32) or `null`. v1.0 presenters compiled against the pre-5.3 protocol decode the new field as unknown and discard it via the asymmetric `deny_unknown_fields` policy (outbound types are permissive on extra fields); v1.0 presenters that hand-typed their own `SessionState` deserialize struct without `last_pid` continue to decode without error. Additive within v1.x. Source: [`crates/protocol/src/state.rs`](../crates/protocol/src/state.rs), [`crates/protocol/src/rest.rs`](../crates/protocol/src/rest.rs). (`Resolves: 5.3`)
- **type: schema** — `Event.pid: Option<u32>` field added AND SQLite `events.pid INTEGER NULL` column added via migration v2 (Story 5.3). The new field rides on every `Event` over the wire (`GET /sessions/{id}/events`, WS `EventFrame.event`); pre-migration rows have `pid = NULL` (the `ALTER TABLE events ADD COLUMN pid INTEGER` migration produces `NULL` for existing rows). Stored `Event` carries the field, but the pre-storage `EventEnvelope` keeps `pid` as an internal field that drives `transition()`'s `last_pid` carry-forward. `INSERT_EVENT` extends from 6 to 7 positional params (`pid` last); `SELECT_EVENT_BY_ID`, `SELECT_EVENTS_FOR_SESSION_SINCE`, `SELECT_EVENT_KINDS_FOR_SESSION` all gain `pid` in their column lists. The migration is idempotent — re-running `to_latest` against an already-migrated DB is a no-op (`PRAGMA user_version` records the applied version). v1.0 presenters decode the new `pid` field via the asymmetric serde policy and discard it; their existing handlers work unchanged. Source: [`crates/protocol/src/event.rs`](../crates/protocol/src/event.rs), [`crates/daemon/src/db/migrations.rs`](../crates/daemon/src/db/migrations.rs), [`crates/daemon/src/db/queries.rs`](../crates/daemon/src/db/queries.rs). (`Resolves: 5.3`)
- **type: schema** — `SessionCurrentState::Ended` + `EventKind::SessionEnded` + `NotificationType` enum added (Story 5.3). `SessionCurrentState::Ended` is the daemon-observed liveness state ("the session's `last_pid` is no longer a live OS process"); **non-terminal** — a subsequent hook event transitions out via `transition()`'s normal arms (e.g. `UserPromptSubmit` from `claude --resume` → `Working`). `EventKind::SessionEnded` is the mechanical event emitted by the daemon's probe; `events.payload` for these rows is `{"reason": "no_pid_at_upgrade" | "pid_dead", "pid": <number or null>, "observed_at_ms": <epoch_ms>}`. New `NotificationType` enum carries the typed Claude Code `notification_type` payload field (`PermissionPrompt`, `IdlePrompt`, `AuthSuccess`, `ElicitationDialog`, `ElicitationResponse`, `ElicitationComplete`, `Unknown` catch-all); wire form is snake_case via `#[serde(rename)]`. The enum is internal to `EventEnvelope` — drives projection transitions but is NOT surfaced on `SessionState` / `StateFrame` (per ADR 0004 §3, the typed value stays in `events.payload` for archaeology and richer presenter rendering on the events stream). v1.0 presenters decode the new variants as `Unknown` via the Story 4.4 `#[serde(other)]` catch-all; the events still parse and the connection stays open. Additive within v1.x. Source: [`crates/protocol/src/event.rs`](../crates/protocol/src/event.rs), [`crates/protocol/src/state.rs`](../crates/protocol/src/state.rs). (`Resolves: 5.3`)
- **type: behavioral** — `Notification → WaitingInput` mapping refined to be `notification_type`-aware (Story 5.3). Previously every `Notification` event flipped `current_state` to `WaitingInput` (Story 1.6); the maintainer's dogfooding deck surfaced ~48 sessions stuck in `WaitingInput`, all `idle_prompt` notifications (a "yo I'm idle" beep, not "give me input"). New rule: input-required `notification_type` values (`permission_prompt`, `idle_prompt`, `elicitation_dialog`) transition to `WaitingInput`; transient values (`auth_success`, `elicitation_response`, `elicitation_complete`), the additive-compat `Unknown` value, and a missing `notification_type` field all preserve prior `current_state` (with `last_event_kind` / `last_event_at_ms` still updating to reflect the event). Despite the name `idle_prompt`, Claude Code's idle pings are surfaced through this hook and ARE classified as input-required — Claude is genuinely waiting for the user to come back. Note: this is a refinement on top of Story 5.2's `PostToolUse → preserve prior` rule, which also lands in 5.3 as a behavioral change (see next entry). v1.0 presenters consuming `state.session.*` see fewer spurious `WaitingInput` transitions; no schema change. Source: [`crates/daemon/src/projection/state.rs`](../crates/daemon/src/projection/state.rs). (`Resolves: 5.3`)
- **type: behavioral** — `PostToolUse → Working` unconditionally (Story 5.3). Refines Story 5.2's "preserve prior" rule: a session in `WaitingInput` whose tool call completes mid-elicitation now correctly transitions back to `Working` rather than staying stuck. The Story 5.2 rule preserved prior on the rationale "the agent is alive between tool calls"; that rationale still holds for the `Working → Working` case, but masked the `WaitingInput → Working` case. The new unconditional rule covers both. v1.0 presenters see strictly more `state.session.*` transitions through `Working` for sessions that visited `WaitingInput`; no schema change. Source: [`crates/daemon/src/projection/state.rs::transition`](../crates/daemon/src/projection/state.rs). (`Resolves: 5.3`)
- **type: behavioral** — Ingest-socket contract: shim injects `bowerbird_ppid`, adapter extracts `notification_type` (Story 5.3). The shim's hot-path code now calls `libc::getppid()` (the only `unsafe` block in `crates/shim/src/main.rs`; the workspace `unsafe_code` lint was downgraded from `forbid` to `deny` to accommodate this one call site per the project-context.md spec) and injects the resulting integer as a top-level `bowerbird_ppid` field on every payload sent through the ingest socket. The shim does NOT extract `notification_type` — that's the adapter's job. The `adapter-claude::normalize` step reads `bowerbird_ppid` (yielding `EventEnvelope.pid: Option<u32>`) and `notification_type` (only for `kind = Notification`; yielding `EventEnvelope.notification_type: Option<NotificationType>`). A missing, non-integer, out-of-range, or zero `bowerbird_ppid` yields `pid = None` without failing normalization (PID 0 is rejected at the adapter boundary because `kill(0, 0)` has process-group rather than single-process semantics, which would make the liveness probe report the session immortal — Story 5.3 review finding #4). A custom producer that wants the daemon to track liveness MUST inject `bowerbird_ppid`; without it the session's `last_pid` stays `None` and the startup probe emits `SessionEnded` with `reason: "no_pid_at_upgrade"` on the next daemon restart. The shim hot-path p99 ≤5ms budget (Story 1.5) is preserved — `serde_json::Number::from(i32)` is zero-allocation, and the syscall is signal-safe. v1.0 presenters are unaffected; the ingest contract documented in `docs/protocol.md` §Ingest socket contract grows two bullets. Source: [`crates/shim/src/main.rs`](../crates/shim/src/main.rs), [`crates/adapter-claude/src/normalize.rs`](../crates/adapter-claude/src/normalize.rs). (`Resolves: 5.3`)
- **type: behavioral** — `GET /sessions/{id}/events` now returns `404 Not Found` for a session_id that has never existed in `session_projections`, matching the shape `/sessions/{id}` and `/sessions/{id}/stats` already use. Previously the endpoint returned `200 {"events":[],"cursor":null,"oldest_available_event_id":<i64::MAX>}` for any id including typos. Presenters polling `/events?since=0` against an unknown id now see `404` immediately rather than silent empty; `bowerbird export` drops its pre-check `GET /sessions/{id}` round trip and lets the events endpoint's own `404` surface the not-found case. v1.0 presenters that intentionally polled empty sessions to wait for the first event must switch to `GET /sessions/{id}` for existence checks (which already returned `404` for unknown) and only call `/events` once existence is confirmed. (`Resolves: 5.4`)
- **type: behavioral** — `idle_prompt` reclassified out of input-required (Story 5.6, ADR 0005). `idle_prompt` no longer transitions a session into `SessionCurrentState::WaitingInput`. Its rule (refined in code review): **`idle_prompt → Idle`, except a session already in `WaitingInput` stays `WaitingInput`** (a pending `permission_prompt`/`elicitation_dialog` block is neither created nor cleared by an idle nudge). `permission_prompt` and `elicitation_dialog` (incl. `AskUserQuestion`) are now the ONLY `notification_type` values that transition a session INTO `WaitingInput`. Rationale: `idle_prompt` is the idle nudge Claude Code fires ~60s after a turn ends (`Stop → Idle`), not a block; classifying it as input-required made the idle nudge a one-way ratchet (the longer a finished session sat, the more "needs-you" it looked). The maintainer's live deck on 2026-05-29 showed 13 of 15 live sessions stuck in `WaitingInput`, none actually blocked. Resolving to `Idle` (rather than preserving a prior `Working`) also covers a **dropped `Stop`**: a finished session whose `Stop` was lost lands on `Idle` on the next idle nudge instead of pinning a stale `Working` that the nudge's timestamp refresh would keep alive past the read-time stale-`Working` fallback. **This supersedes the `idle_prompt` classification in the Story 5.3 `Notification → WaitingInput` behavioral entry above** (which asserted "idle pings ... ARE classified as input-required"); that entry stays in the append-only history. Relatedly, the truly-transient notification types (`auth_success` / `elicitation_response` / `elicitation_complete` / unknown / missing) still preserve prior `current_state`, EXCEPT a prior `Ended` resurrects to `Idle`: a notification hook arriving is evidence the process is alive (consistent with ADR 0004's non-terminal-`Ended` contract). No wire-format change, no migration, no field change: `NotificationType` keeps all seven variants. v1.0 presenters see strictly fewer `WaitingInput` transitions; old presenters decoding with `#[serde(other)]` are unaffected. **Existing `WaitingInput` rows the pre-5.6 daemon wrote from an `idle_prompt` are NOT auto-remediated** — this is a future-event-only change; a stuck row clears only when the session receives a hook that transitions out of `WaitingInput` (`UserPromptSubmit`/`PreToolUse`/`PostToolUse` → `Working`, `Stop` → `Idle`, or the daemon's `SessionEnded` → `Ended`); a later `idle_prompt` or other transient notification leaves it `WaitingInput`. Operators draining a pre-5.6 wall fix the local `bower.db` by hand or truncate it; a general non-schema data-migration facility is deferred (see `docs/bmad/implementation-artifacts/deferred-work.md`). Source: [`crates/daemon/src/projection/state.rs::transition`](../crates/daemon/src/projection/state.rs), [ADR-0005](decisions/0005-idle-prompt-transient-not-input-required.md). (`Resolves: 5.6`)
- **type: schema** — `SessionState.cwd` + `Event.cwd` + `SessionListItem.cwd` and `SessionState.started_at` + `SessionListItem.started_at` fields added (Story 5.7, ADR 0006). **`cwd: Option<String>`** is the session's working directory as the source's hook payload reported it, stored verbatim (no path canonicalization, no `~` expansion, no symlink resolution) and carry-forwarded across events (`cwd.or(prev.cwd)`, overwrite-on-Some — identical to `last_pid`). Unlike `bowerbird_ppid` (shim-injected), `cwd` is a NATIVE Claude Code hook field present at the top level of every payload, so there is **no shim change** — the `adapter-claude` normalize step reads it; a missing or non-string `cwd` yields `None` without failing normalization. `cwd` rides every stored `Event` over the wire (`GET /sessions/{id}/events`, WS `EventFrame.event`) and `SessionState` / `SessionListItem` / `SessionDetail.state`. A nullable `events.cwd TEXT` column is added via SQLite **migration v3** (`ALTER TABLE events ADD COLUMN cwd TEXT`; idempotent; pre-v3 rows get `cwd = NULL`; `PRAGMA user_version` → 3) so projection rebuild reads a typed column rather than re-parsing source-specific payload shape. **`started_at: Option<i64>`** (epoch ms) is on `SessionState` and `SessionListItem` only — NOT on `EventEnvelope`/`Event` (it is a session-level projection fact). It is daemon-derived and set once to the session's first observed event's `created_at`, never overwritten (`prev.started_at.or(Some(now_ms))` — the INVERSE direction from `cwd`'s overwrite-on-Some). It needs no payload field, no adapter code, no `events` column, and **no migration**: it is derivable, reconstructs identically on rebuild (events replay oldest-first), and pre-5.7 projection blobs lacking the field deserialize to `None` (serde defaults absent `Option` fields, same mechanism `last_pid` relied on in 5.3). *repo*/*project*/*branch* (from `cwd`) and session age (from `started_at`) are presenter derivations, never daemon fields (Axiom 4). v1.0 presenters compiled before Story 5.7 decode both fields and discard them via the asymmetric permissive-outbound serde policy — no decode error, no protocol-version bump within v1.x. Source: [`crates/protocol/src/state.rs`](../crates/protocol/src/state.rs), [`crates/protocol/src/event.rs`](../crates/protocol/src/event.rs), [`crates/protocol/src/rest.rs`](../crates/protocol/src/rest.rs), [`crates/daemon/src/db/migrations.rs`](../crates/daemon/src/db/migrations.rs), [ADR-0006](decisions/0006-session-cwd-on-the-wire.md). (`Resolves: 5.7`)
